# 物流专线查询小程序 PRD

> **版本**：v6.4（对应当前代码实现）
> **最后更新**：2026-09-22
> **上线状态**：✅ **已于 2026-09-21 通过微信审核并正式上线**（v6.0 ~ v6.4 均为上线后改动，**需重新提交审核**）
> **文档定位**：以**现有代码实现**为准反向整理的产品需求文档，作为后续开发、迭代、审核以及多人协作的基准依据。与 README（v3.1 介绍性文档）冲突时，**以本 PRD 为准**。
>
> **v6.4 变更（信息纠错改为站内表单）**：用户提供参考图，要求「点击'信息有误点这里报错'，弹出该修改页面」。
> - **旧流程的致命问题**：弹框说明 → ActionSheet 让用户打电话/加微信，**用户必须跳出小程序切到另一个 App** 才能反馈，
>   摩擦大到几乎无人使用；且反馈内容只存在于客服的口头记录里，**没有结构化留存**，无法统计"哪家公司被反复报错"。
> - **新流程**：新建 `pages/report` 站内表单（问题类型 chips + 具体说明 200 字 + 最多 3 张凭证 + 选填联系方式），
>   提交经云函数 `reportFeedback` 落 `feedback` 集合，后台集中核实。
> - **新增云函数 `reportFeedback`**：服务端做类型白名单反查、全部字段长度再截断、单用户单日 10 条频控、`_openid` 显式注入。
>   ⚠️ **必须上传部署**，否则提交报"云函数未部署"。
> - **入口 3 处**：专线详情 + 公司详情（带参，标明被反馈对象）+「我的」菜单「信息纠错」（不带参）。
> - **新增回归测试** `.workbuddy/scripts/test-reportfeedback.js`（**61 断言**）。页面数 17 → **18**。
> - **详见 F-REPORT-01 / F-REPORT-02**。
>
> **v6.3 变更（网点分组修正 · 用户报障）**：用户反馈「电话不需要排序。发站电话对应发站电话，有多个发站地址，或者多个到站地址进行排序」，
> 附截图显示发站被渲染成「① 真实地址 / ② 地址未填写 / ③ 地址未填写」。
> - **根因**：数据本身没错（确为 1 个地址 + 3 个号码），是**读端按行逐条成组**导致 1 个地址被拆成 3 个"网点"。
>   这与 R-13 第 2 条「单地址多电话保持 1 网点挂多号」**直接冲突**——写入端有护栏，读端没有。
> - **修正**：纯电话行不再各自成组，统一并入第一个地址组；**前端取消渲染「地址未填写」**；
>   统计卡与「共 N 个」改为只数**有地址的组**；**取消 `splitPhones` / `splitAddrs` 的 `.sort()`**（打乱录入顺序）。
> - **范围**：专线详情（`pages/line-detail`）+ 公司详情（`pages/company-detail` + `cloudfunctions/searchCompany`）**两页同改**。
> - **顺带**：公司详情的站点电话由"多号塞进一个胶囊、点击后再选"改为**逐号渲染、点击直拨**（与专线详情同交互，少一步）。
> - **新增回归测试**：`.workbuddy/scripts/test-outlet-pairing.js`（30 断言）；`test-searchcompany.js` 48 → **59 断言**。
> - **详见 R-13 的「读端也必须遵守"单地址多电话 = 1 组"」小节。**
>
> ⚠️ **云函数待重传**：`searchCompany` 本版有改动（`collectStations` 分组逻辑），**必须重新上传部署**才生效。
>
> **v6.2 变更（全站移除毛玻璃）**：用户要求「全部字体删除毛玻璃效果」，范围含管理端。
> - **删除全部 `backdrop-filter`**：19 个文件、86 处（17 个页面/组件 WXSS + `app.wxss`）。执行脚本 `.workbuddy/scripts/remove-glass.js`（幂等、带 `--dry` 与残留自检）。
> - **★ 连带必改**：只删模糊、保留 `.88` 半透明会让卡片变成"透着背景渐变的淡灰块"，**比原来更脏**；因此**同时把半透明白底提纯为实心白/近白**（148 处）。
> - **踩坑与修复**：首版迁移脚本无差别提纯，把 `color: rgba(255,255,255,.96)` 也改成近白——那是**深色渐变头图上刻意用的白色文字/占位符**，在白底上会直接消失。脚本已改为**逐行判定属性名，`color` 一律跳过**；同时人工修正了 `index` / `company-list` / `line-query` / `admin/ads` / `admin/ad-edit` 的搜索框与头图文字配色（白色文字改为深色，因为它们所在容器已变白底）。
> - **`--glass-blur` / `--glass-saturate` 废弃**，`.glass*` 类名保留以兼容既有引用。
> - **详情见 R-23**。⚠️ 删除"页面根节点必须 `background: transparent`"的硬约束（该约束仅为 `backdrop-filter` 服务），但**写法保留**——天空渐变画在 `page` 元素上，根节点透明才能看见。
>
> **v6.1 变更（详情页视觉统一 · 按参考图还原）**：依据「齐鲁快运 公司详情」参考图，把**专线详情与公司详情统一到同一套卡片语言**。
> - **确立两套卡片语言**（见 R-23）：**浏览型页面**（首页/查专线/列表/查公司/收藏/我的）继续用磨砂 + 渐变头图；**档案型页面**（专线详情/公司详情）改用**白底描边卡打头，不用渐变块**。理由：详情页信息密度最高、阅读最久，半透明底会削弱正文可读性。
> - **新增全局 `pf-*` 卡片体系**（写入 `app.wxss`）：头卡（公司名 + 已核实 + 元信息行）、站点卡（蓝发站 / 橙到站）、简介卡、三统计、分组线路、纠错按钮、兜底态。**两个详情页共用，页面不得覆盖其色值**（防再次漂移）。
> - **公司详情（F-COMPANY-02）**：按参考图逐项还原；头部由渐变头图改为白底描边卡。
> - **专线详情（F-DETAIL-01）**：头卡同构；三统计由「时效/价格/方式」（低填写率，首屏大量 `—`）改为**「发站网点 / 联系电话 / 到站网点」**（派生字段，必然有值）；底部操作栏补回分享 + 收藏。
> - **新增元信息能力**（见 R-18）：`updateText`（由 `_id` 的 ObjectId 时间戳反推，数据里完全没有时间字段）、`viewCount`（`stat_events` 聚合，可失败且不得影响主流程）。**两项均条件渲染，无值不显示。**
> - **移除冗余交互**：专线详情的「网点拨号按钮」（`onCallGroup`）删除——号码胶囊本身即可直拨。
> - **列表页公司卡**：`已核实` 徽章由蓝底统一为绿底（与详情头卡一致）。
> - **不变**：全部数据一致性护栏（R-10~R-18）、搜索两级查询、埋点频控、收藏分页拉全、空广告位不渲染等合规逻辑**一行未改**。
>
> **v6.0 变更（信息架构重构 · 全站重设计）**：依据 8 张参考图重构**信息架构**，用户全权委托。
> - **tabBar 由「首页 / 收藏 / 我的」改为「首页 / 查专线 / 查公司」**；收藏移入查专线页、我的移入查公司页。
> - **首页改为分流页**：大标题 + 搜索框 + 两个入口卡（我要发货 / 找物流公司）+ 免责声明・隐私政策页脚。原首页的双输入框查询下沉为独立「查专线」页。
> - **新增模块**：查专线页（地址双卡 + 筛选 chip + 空态，取代原 `picker mode="region"` 三列滚轮）、查公司页、公司详情页、地区选择半屏弹层组件（`components/region-picker`）、`searchCompany` 云函数。
> - **新增能力**：列表页筛选（只看直达 / 天天发车 / 时限）、专线列表页筛选条、纠错入口（专线详情 + 公司详情）。
> - **新增文档**：免责声明（`AGREEMENTS.disclaimer`），与用户服务协议、隐私政策并列。
> - **地区数据建立"单一源 + 生成副本"机制**：`regionData.js` 为唯一数据源，脚本生成前端 `utils/region-data.js`，含自检。
> - **不变**：全部数据一致性护栏（R-10~R-15）、搜索两级查询、埋点频控、收藏分页拉全、空广告位不渲染等合规逻辑**一行未改**。
>
> **v4.3 变更****专线详情页（F-DETAIL-01）按真实数据填写率重排**。依据对 475 条真实线路的字段统计，把首屏主打的「时效/价格/方式」（填写率仅 42-62%）降级为条件渲染的「运营信息」区块，把此前未展示的「覆盖区域」（74.3%）提为首个内容区块；底部操作栏由三按钮改为「导航 + 立即拨打」两按钮，分享移至头部与收藏并列。同版本记录**供给侧风险**：`toPhone` 仅 43.2%，约四成线路无法直接拨号。
>
> **v4.2 变更**：记录**上线状态**；§8.4 改为「上线状态与待办」，逐项归集上线时点的合规检查结论（服务类目 ✅、协议主体 ✅、数据库索引 ✅、基础库版本 ⚪ 不再处理；隐私保护指引 🟡、数据来源 🟡 留待跟进）。
>
> **v4.1 变更**：**移除「月度兑奖」活动**（用户端兑奖页、管理端兑奖管理、`prize` 云函数及相关入口/索引）。原因：小程序注册主体为**个人**，个人主体不可选择需要资质的服务类目，且抽奖类活动为微信审核敏感项。移除后小程序回归纯"信息查询工具"定位，与服务类目对齐，消除审核风险。
>
> 同版本另补：**协议已载明运营主体信息**（陈镇，个人开发者）与联系方式，满足隐私政策对"个人信息处理者"的法定披露要求。

> ⚠️ **上线后变更纪律**：任何代码改动都需**重新提交审核**方可生效；改动前先在本 PRD 定位需求编号，改动后同步更新对应条目。**不要在生产配置上做"顺手优化"。**

---

## 0. 文档使用约定

| 项 | 约定 |
|---|---|
| 需求编号 | `F-{模块}-{序号}`（功能）、`R-{序号}`（非功能/规则） |
| 状态标记 | ✅ 已实现 ｜ 🟡 部分实现 ｜ ⬜ 规划中 |
| "已实现"定义 | 代码已落地且链路完整（前端 + 云函数/数据库） |
| 变更要求 | 任何开发动工前，先在本 PRD 定位对应需求编号；新增能力需先补条目再写代码 |

---

## 1. 产品概述

### 1.1 一句话定义
面向**货主/发货方**的物流专线查询工具：输入出发地与目的地，即可查到匹配的物流专线、公司信息、时效价格、**发货/到货网点地址与电话**，并支持一键拨号、地图导航、收藏与分享。

### 1.2 解决的问题
- 传统方式找专线依赖熟人介绍、微信群翻记录，信息零散且无法按线路检索。
- 专线公司信息（多个发货点、多个联系电话）分散，货主难以快速对接到**正确的网点与号码**。
- 无直达线路时，货主不知道可以**经中转城市接驳**，往往直接放弃。

### 1.3 目标用户
| 角色 | 描述 | 核心诉求 |
|---|---|---|
| 货主（C 端用户） | 有发货需求的个人/小商家 | 快速找到能到自己城市的专线，拿到电话直接联系 |
| 平台管理员 | 平台运营方（单人/小团队） | 批量维护线路数据、售卖广告位、发布公告、看数据 |

### 1.4 商业模式
- **广告位售卖**：首页 10 个公司卡片位，按位出售给物流公司，点击可跳转对应专线详情。
- **会员（VIP）**：按**线路**维度售卖的付费权益，有效会员在搜索结果中**置顶**并获得金色"会员"徽标 + 流光动效；按公司可批量开通/续费。

> **已下线**：「月度活跃榜兑奖」活动已于 v4.1 移除，原因见文档头部变更说明。统计数据（`stat_events`）保留，但用途回归为**平台自用的使用量统计**，不再用于任何发奖活动。

### 1.5 主体与服务类目（合规前置条件）
- **注册主体：个人**。
- 个人主体**不可选择需要资质的服务类目**（如「交通服务 → 物流服务」需营业执照或道路运输资质）。
- 因此服务类目必须落在**「工具」大类下的信息查询类目**（如「工具 → 信息查询」/「商业服务 → 信息服务」），与服务定位一致：平台仅提供专线信息查询，不承运、不参与交易。
- **推论（对功能的约束）**：任何抽奖、发奖、交易类功能都会破坏"纯信息工具"的定位并触发资质要求，**不应再引入**。

### 1.5 合规基线（硬约束）
- ✅ **不收集**用户手机号、姓名等个人身份信息（"发布找线需求"功能已于 v3.1 下线）。
- ✅ 云端仅存储：收藏的专线内容、`openid`（用于收藏归属与管理员识别）。
- ✅ 本地仅存储：查询历史（不上传）。
- ✅ 内置《用户服务协议》《隐私政策》入口（「我的」页）。
- ⚠️ **首页不渲染空白广告位**：仅当广告位已有 `companyName` 时才渲染卡片。这是为规避微信《常见拒绝情形》3.2.2（页面内含空白广告位、招商广告将被拒绝）。
- ⚠️ 隐私声明需覆盖：用户身份信息（openid）、位置信息（发货点设置）。

---

## 2. 技术架构

| 层 | 技术选型 | 说明 |
|---|---|---|
| 前端 | 微信小程序原生（WXML/WXSS/JS） | 无框架，`lazyCodeLoading: requiredComponents` |
| 后端 | 微信云开发 CloudBase | 环境 ID `cloud1-d0ge42roj61603242` |
| 数据库 | 云数据库（NoSQL，文档型） | 客户端 SDK 直读 + 云函数写入 |
| 服务端逻辑 | 云函数（Node.js） | **8 个云函数**，见 §2.2 |
| 分包 | `pages/admin` 独立分包 | 管理端代码不进主包，避免影响用户端启动耗时 |
| 自定义组件 | `components/region-picker` | 地区选择半屏弹层（v6.0 新增） |

### 2.1 目录结构

```
logistics-line-query/
├── app.js / app.json / app.wxss      # 入口、全局配置、设计令牌
├── config/admin.js                   # 前端管理员白名单（仅控制入口显隐）
├── components/
│   └── region-picker/                # 地区选择半屏弹层（省市县三级下钻）
├── cloudfunctions/
│   ├── getOpenid/                    # 获取 openid
│   ├── searchLine/                   # 搜索聚合（直达+中转）
│   │   ├── placeMatch.js             #   地名匹配规则（5 条）
│   │   └── regionData.js             #   省市行政区数据 + getCityLevel（★ 地区数据唯一源）
│   ├── searchCompany/                # 公司维度聚合（按公司名搜 / 公司详情）
│   ├── adminLine/                    # 专线 CRUD / 批量导入 / 批量 VIP
│   ├── adminAd/                      # 广告位管理
│   ├── adminNotice/                  # 公告管理
│   ├── adminStats/                   # 月度统计
│   ├── statLog/                      # 行为埋点（带频控）
│   ├── reportFeedback/               # 信息纠错反馈写入（带频控，v6.4）
│   └── createIndexes/                # 数据库索引定义（含 README）
├── pages/                            # 用户端 11 页
├── pages/admin/                      # 管理端 7 页（分包）
├── utils/                            # 8 个公共模块（见 §2.3）
└── docs/                             # 导入模板、导入说明、设计令牌
```

### 2.2 云函数清单

| 云函数 | 权限 | 职责 | action |
|---|---|---|---|
| `getOpenid` | 公开 | 返回调用者 openid/appId | — |
| `searchLine` | 公开 | 搜索聚合：直达 + 中转 | — |
| `searchCompany` | 公开 | 公司维度聚合：按公司名搜 / 公司详情 | `search` / `detail` |
| `adminLine` | 管理员 | 专线全生命周期管理 | `save` / `batchSave` / `remove` / `toggle` / `list` / `batchVip` / `migrateArrays` / `healIds` |
| `adminAd` | 管理员 | 广告位（1-10 号位）读写 | `save` / `list` |
| `adminNotice` | 管理员 | 首页公告读写 | `save` |
| `adminStats` | 管理员 | 月度活跃用户排名与总量 | — |
| `statLog` | 公开（带频控） | 用户行为埋点写入 | `log` |
| `reportFeedback` | 公开（带频控） | **信息纠错反馈写入**（v6.4 新增） | `submit` |

> **部署注意**：`searchCompany` 为 v6.0 新增，**必须上传**，否则「查公司」整页不可用。
> `searchLine` 因 `regionData.js` 增加导出也**需重新上传**（改动虽不影响运行，但保持云端与仓库一致）。
> `reportFeedback` 为 v6.4 新增，**必须上传**，否则纠错表单提交会提示"云函数未部署"。

### 2.3 公共模块（`utils/`）

| 模块 | 职责 | 关键约束 |
|---|---|---|
| `storage.js` | 查询历史（本地）+ 收藏（云端） | 收藏必须循环分页拉全（单次上限 20 条）；`addFavorite` 幂等 |
| `lineKey.js` | 线路定位键统一解析 | **所有按线路定位的操作必须走此模块** |
| `region-data.js` | 省市县三级数据 + `displayName()` | **自动生成 · 禁止手改**（见 R-16），由 `sync-region-data.js` 从云函数同步 |
| `vip.js` | VIP 有效性统一口径 | `effectiveVip` / `vipStatusText` / `fmtDate` |
| `outlets.js` | 网点（地址↔电话）归一 | 读端统一入口，口径须与 `adminLine.deriveOutlets` 一致 |
| `stats.js` | 埋点封装（fire-and-forget） | 失败只打日志，绝不阻塞用户操作 |
| `ui.js` | 反馈/错误态/防连点锁 | toast 中文 >14 字自动降级为 modal |
| `contact.js` | 客服联系方式配置 | 留空则入口自动隐藏，不出现假号码 |

### 2.4 关键数据模型

#### `lines`（专线，核心集合）
| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | **数据库主键，必有** |
| `id` | number | **业务字段（毫秒时间戳）**，旧数据可能缺失 |
| `title` / `companyName` | string | 线路名称 / 物流公司名 |
| `fromCityName` / `toCityName` | string | 城市名（自然写法，如"济南市"） |
| `fromCity` / `toCity` | string | **搜索索引字段**，由服务端 `normCity()` 派生（去"市/区/县/省/自治州/盟/地区"后缀） |
| `fromAddress` / `toAddress` | string | 地址串（多地址换行分隔） |
| `fromPhone` / `toPhone` | string | 发站/到站电话（多号码逗号分隔） |
| `fromOutlets` / `toOutlets` | Array | **网点精确配对** `[{addr, phone}]` |
| `phone` | string | 兼容字段（= 发站电话），自动派生 |
| `toAreas` / `tags` | Array | 到达辐射区 / 标签（写入端统一归一为数组） |
| `latitude`/`longitude`, `toLatitude`/`toLongitude` | number | 发货点/到货点坐标（导航用） |
| `aging` / `priceDesc` / `lineType` | string | 时效 / 价格描述 / 业务类型 |
| `isVip` | number | 0/1，**由 `vipExpireAt` 派生** |
| `vipExpireAt` | number | **会员到期毫秒时间戳，收费与续费的事实来源**；0/缺失=旧数据 |
| `certified` | number | 0/1 已认证 |
| `status` | number | 1=上架 0=下架（缺失时自愈补 1） |

#### 其他集合
| 集合 | 用途 | 关键字段 |
|---|---|---|
| `favorites` | 收藏（用户隔离） | `lineId`、`line`（专线快照）、`createTime`、`_openid`（系统注入） |
| `ads` | 广告位 | `id`(1-10)、`companyName`、`region`、`number`、`lineId` |
| `notice` | 首页公告 | **固定 `_id = 'current'`**（幂等 upsert） |
| `stat_events` | 行为埋点（PV 口径） | `type`(search/view)、`key`、`month`(yyyy-MM)、`day`(yyyy-MM-dd)、`ts`、`_openid` |
| `app_config` | 系统配置 | `admins`(openids 数组)、`migrated_arrays_v1`(迁移进度) |
| `app_errors` | 线上错误上报 | `source`、`msg`、`page`、`ts` |
| `feedback` | **信息纠错反馈**（v6.4 新增） | `type`/`typeLabel`、`target`/`targetKey`/`targetKind`、`detail`、`images[]`、`contact`、`status`(0=待处理)、`month`、`day`、`ts`、`_openid` |

---

## 3. 功能需求 — 用户端

### 3.1 页面总览

> **v6.0 信息架构**：tabBar 为「首页 / 查专线 / 查公司」三 tab。收藏与我的**不再是 tab**，
> 分别从「查专线」页与「查公司」页的头图右上角进入（`navigateTo`，非 `switchTab`）。

| 页面 | 路径 | 定位 | Tab |
|---|---|---|---|
| 首页 | `pages/index/index` | **分流页**：一句话搜索 + 两个业务入口 + 免责声明/隐私政策 | **Tab 1** |
| 查专线 | `pages/line-query/index` | **按地址查专线**：地址双卡 + 筛选 + 空态（含收藏入口） | **Tab 2** |
| 查公司 | `pages/company-list/index` | **按公司名搜公司**（含我的入口） | **Tab 3** |
| 公司详情 | `pages/company-detail/index` | 公司聚合视图：发站/到站/统计/覆盖线路/纠错 | — |
| 专线列表 | `pages/line-list/index` | 搜索结果（直达/中转）+ 筛选条 | — |
| 专线详情 | `pages/line-detail/index` | 线路全信息 + 拨号/导航/收藏/分享/纠错 | — |
| 收藏 | `pages/favorite/index` | 云端收藏列表（从查专线页进入） | — |
| 我的 | `pages/user/index` | 查询历史 + 管理入口 + 协议/纠错入口（从查公司页进入） | — |
| **信息纠错** | `pages/report/index` | **站内反馈表单**（v6.4 新增，从两个详情页或「我的」进入） | — |
| 联系客服 | `pages/contact/index` | 在线客服/电话/微信/二维码 | — |
| 协议 | `pages/agreement/index` | 用户服务协议 / **免责声明** / 隐私政策 | — |

**自定义组件**

| 组件 | 路径 | 定位 |
|---|---|---|
| 地区选择弹层 | `components/region-picker/index` | 省市县三级半屏弹层，面包屑下钻 + 「不选区县=全市」 |

**公共模块补充**：`utils/region-data.js`（**自动生成**，地区数据前端副本，见 R-16）

---

### F-HOME-01 首页分流入口 ✅

**描述**：首页不承担查询逻辑，只做**分流**——一句话搜索 + 两个业务入口卡。
（v6.0 前首页承担双输入框查询，已下沉为 F-QUERY-01「查专线」页。）

**交互规则**
1. 头部大标题「找专线 · 找物流公司」，副标题「覆盖山东及华东地区专线信息」。
2. **搜索框**：placeholder「输入城市或公司名，如 济南 / 鲁通物流」。
   - 有输入时：显示 ✕ 清除按钮，并显示提示行「将按公司名查询其线路」。
   - 提交（回车/确认）：语义固定为**按公司名查线路**，跳查公司 tab。
   - **跳转方式（关键）**：目标是 tabBar 页，必须用 `wx.switchTab`，而 tabBar 页**不能带 URL 参数** → 关键词经 `app.globalData.pendingCompanyKeyword` 交接，由 `company-list` 的 `onShow` 读取并**立即清空**（防止用户下次切回该 tab 时重复触发搜索）。关键词为空时 toast 拦截。
3. **两个业务入口卡**（左右并排，各占一半）：
   - 橙卡「我要发货 / 按出发地目的地查」→ `switchTab` 到查专线（Tab 2）；
   - 蓝卡「找物流公司 / 按公司名查线路」→ `switchTab` 到查公司（Tab 3）。
4. **页脚**：「免责声明 ｜ 隐私政策」两个可点链接（跳 `pages/agreement`），下方固定声明「信息由公开渠道整理，仅供参考，请自行核实」。
5. 防连点：搜索用 `ui.lock(this,'search', false)` 静默拦截。

---

### F-QUERY-01 按地址查专线（Tab 2）✅

**描述**：选择出发地与目的地，查跑这条线的所有物流公司。v6.0 新增，取代原首页的双输入框。

**页面结构**
1. 头图：标题「按地址查专线」+ 副标题；右上角**收藏入口**（收藏已移出 tabBar）。
2. **地址双卡**：出发地（浅蓝）+ 目的地（浅橙），中间「⇄」交换按钮。
   - 未选时显示占位「请选择」/「请选择省/市/区县」。
   - 点击任一卡片 → 打开地区选择弹层（F-REGION-01）。
3. **筛选行**（三个胶囊 chip）：
   - 「只看直达」——勾选框样式，选中为实心主色；
   - 「天天发车」——同上；
   - 「不限时效 ▾」——ActionSheet 选择 1/2/3 天内，**选择结果持久化**到本地（货主时效习惯固定）。
4. 「查询专线」按钮：地址未选齐时置灰但**仍可点击**（点击给 toast 引导，避免"点了没反应"）。
5. 内容区随状态切换：未选地址 → 空态卡「选择出发地和目的地 / 即可查看跑这条线的所有物流公司」；已选齐 → 就绪卡显示路线并说明「将按出发城市与到达城市匹配直达线路，未找到直达时会给出中转方案」。
6. 底部「⌂ 返回首页」按钮。

**查询行为**
- 校验：出发地/目的地为空时 toast 拦截。
- 防连点：`ui.lock(this,'query',false)`。
- 写入本地查询历史（`fromCityName`/`toCityName`）+ 埋点 `stats.logSearch`。
- 跳转 `pages/line-list`，参数：`fromCity/fromText/toCity/toText` + 筛选 `direct=1` / `daily=1` / `maxDays=N`（未选时不传），均 `encodeURIComponent`。

**与 F-SEARCH-01 的关系**：本页只负责**收集条件并跳转**，搜索逻辑仍全部在 `searchLine` 云函数；筛选**不下发服务端**，由列表页客户端过滤（见 F-LIST-02）。

---

### F-REGION-01 地区选择弹层（`components/region-picker`）✅

**描述**：省市县三级半屏弹层，取代原 `picker mode="region"` 三列滚轮。v6.0 新增，参考图明确要求。

**交互规则**
1. **逐级下钻，而非一次性三段选择**：省 → 市 → 区县，每次点选进入下一级。
2. **面包屑**：已选路径（省 › 市 › 区县）显示在列表上方，**可点击回退**到对应层级。
3. **标题行**：左「取消」/ 中标题（如「选择目的地（省/市/区县）」）/ 右「确定」（主色加粗）。
4. **「不选具体区县，直接点右上确定表示全市」** —— 底部固定提示。这是货主最常用的粒度（关心"到南充"而非"到南充市高坪区"）。
5. 选中项显示 ✓；有下级的项显示 ›。
6. 遮罩点击 = 取消；弹层内点击不穿透（`catchtap`）。
7. 每次打开从「省」这一级重新开始，已选值作为初始路径铺开（回显）。
8. **确定时**：只选了省 → 语义为"全省"，仍允许提交。

**数据与口径（关键护栏 R-16）**
- 数据来源 `utils/region-data.js`，该文件由 `cloudfunctions/searchLine/regionData.js` **自动生成**（单一源），确保**展示的地区树与搜索匹配用的行政区划表是同一份**。
- **展示名 vs 查询名分离**：区划表存规范短名（如「顺庆」），展示需补后缀（「顺庆区」）。`displayName()` 用**显式映射表**而非正则推断 —— 因为「乌鲁木齐县」的「县」是名称本身的一部分。
- **直辖市没有中间"市"层**：区县直接挂在直辖市下（如 重庆 › 万盛）。
- **同名多地**（市中 / 城区 / 东区 / 西区 / 海南 / 朝阳 / 吉林）由逐级下钻的**父级上下文**天然消歧。
- 抛给页面的结果含：`province/city/county/text`（展示文本）+ `queryCity`（查询用城市字段，区县存在时取其所属市，与搜索索引的市级口径一致）。

---

### F-COMPANY-01 查公司（Tab 3）✅

**描述**：按公司名搜索，返回公司卡片列表。v6.0 新增模块。

**页面结构**
1. 头图：标题「查公司」+ 副标题；右上角**我的入口**（已移出 tabBar）。
2. 搜索行：输入框（placeholder「输入公司名，如 齐鲁 / 鲁通物流」，有值时显示 ✕）+ **「搜索」按钮**。
3. 结果区：
   - 搜索后显示「找到 N 家公司」计数。
   - 公司卡片：**公司名 + 「已核实」徽章**（仅 `certified` 时）+ 元信息行「主要出发城市 · 规模 · N 条线路」+ 右侧 ›。
   - 未搜索 → 引导态「输入公司名开始查询 / 支持公司简称，如输入齐鲁可查到山东齐鲁快运有限公司」。
   - 无结果 → 「没有找到「XX」相关公司」+ 建议改用简称或走查专线。
   - 失败 → 错误态 + 重试按钮（与"没找到"严格区分）。
4. 页脚声明「信息由公开渠道整理，仅供参考，请自行核实」。

**搜索行为**
- 关键词为空 toast 拦截；防连点 `ui.lock(this,'search',false)`（用 `complete` 统一解锁，避免异常分支漏解锁导致按钮永久锁死）。
- 埋点 `stats.logSearch(kw, 'company')`。
- 调 `searchCompany` 云函数 `action:'search'`。
- 支持**简称命中全称**（如「齐鲁」→「山东齐鲁快运有限公司」）；排序：完全相等 > 前缀命中 > 线路数降序。
- 关键词经正则元字符转义，输入 `(` 等不崩溃。

---

### F-COMPANY-02 公司详情 ✅

**描述**：一家公司的聚合视图。v6.0 新增，v6.1 按参考图**逐项还原视觉**。

**页面结构**（v6.1：白底描边头卡打头，**不再用渐变头图**）
1. **头卡**（白底 + 细描边 + 小圆角，与专线详情共用 `pf-head-card`）：
   - 公司名（36rpx 主标题）+ 「已核实」绿底胶囊（`certified` 为真才渲染）。
   - 元信息行（`·` 分隔，**每项均为条件渲染，无值整项不出现**）：
     「主要出发城市」·「规模」·「更新: N天前」·「N次浏览」·「会员线路 N 条」。
     - `updateText` 由 `_id` 的 ObjectId 时间戳反推（见下「元信息口径」），无法推导或超 90 天则不显示。
     - `viewCount` 取自 `stat_events` 的 `view` 事件计数，为 0 则不显示。
2. **发站信息卡**（浅蓝，标题右侧「共 N 个」）：编号方块 + 地址 + 「发站电话 号码胶囊」（点击任意号码直接拨打）。
3. **到站信息卡**（浅橙，同构）。
   - **站点无地址时不渲染该行**（v6.3：不再显示「地址未填写」占位）；**无电话时不渲染拨号区**（不造按钮）→ 需求编号 `R-COMPANY-02-A`。
   - **一个地址挂多个号码时逐号渲染成胶囊**（`pf-st-phones` 容器 + `wx:for`），点哪个拨哪个（`onCallOne`）→ v6.3：**不再用 ActionSheet 二次选择**，与专线详情页交互统一。
   - 空数组时**整卡不渲染**（不留空标题）。
   - **「共 N 个」只数有地址的站**（`fromAddrCount` / `toAddrCount`），且为 0 时整个「共 N 个」标签不出现（v6.3，与 F-DETAIL-01 同口径）。
4. **公司简介**：**不编造资质/历史**，只描述可见事实（覆盖城市数、主力线路城市及直达条数），末尾固定「以上信息由公开渠道整理，具体运价与时效请与承运方确认」。无内容整卡隐藏。
5. **三统计**：出发城市 / 覆盖线路 / 到达城市（大数字，三色；中间细分隔线）。
6. **覆盖线路**（区块标题右上「按出发城市分组」）：
   - 分组头「城市 + N 条」（浅灰底胶囊）；组内每行「到达城市 + 标签（直达/中转 · 时效 · 天天发车）+ ›」。
   - 组间按线路数降序（主力出发地优先），组内按到达城市排序。
   - 点击行 → 专线详情页，`lineId` 取 `_id` 优先（与 R-10 双键制、`lineKey.lineRef` 口径一致）。
7. **纠错入口**「信息有误？点这里报错」→ modal 说明 + ActionSheet（拨客服电话 / 复制客服微信）。
8. 页脚声明同 F-COMPANY-01。

**聚合口径（关键，见 R-17）**
- **不建独立 companies 集合**：所有字段由在营线路实时聚合，避免双份数据不一致。
- `status === 0`（已下架）**不计入统计**。
- VIP 有效数用 `isVip === 1 && (无 vipExpireAt || vipExpireAt > now)`，与 R-15 一致。
- 「已核实」= 任一在营线路 `certified === 1`。
- 规模按线路数粗分：≥20 大型 / ≥8 中型 / 其余小型。

**元信息口径（v6.1 新增，见 R-18）**
- `updateText`：线路文档**没有任何时间字段**（实测 475 条导入数据 0 命中，`adminLine` 白名单亦不含），
  故由 `_id` 的 **ObjectId 前 4 字节（前 8 位十六进制）= 创建时间秒** 反推，取该公司在营线路中最新的一条。
  - `_id` 非 24 位十六进制（手工指定主键的历史数据）→ 返回 `''`。
  - 时间戳不在 2015~2100 合理区间 → 返回 `''`。
  - 相对文案超过 **90 天** → 返回 `''`（不显示「286天前」这类无意义信息）。
- `viewCount`：`stat_events` 中 `type='view'` 且 `key ∈ 该公司线路 id` 的事件数。
  - **仅取前 30 个线路 id**（`VIEW_ID_LIMIT`）；线路更多时放弃统计返回 0。
    原因：`stat_events` 只有 `month_openid` / `day_openid` / `month_type` 三个索引，
    没有 `type+key` 索引，`_.in(大数组)` 会退化为扫描。
  - **任何异常（集合不存在 / 超时）均吞掉返回 0**，绝不影响详情主数据返回。

**v6.3 变更说明（网点分组口径修复）**
- **根因**：`searchCompany` 的 `collectStations` 按行逐条 push 成站，一行「纯电话」也被当作一个站点 → 1 个地址 + N 个纯电话行会被渲染成 N+1 个站，多个站无地址（旧版显示「地址未填写」）且电话被拆散。
- **修复**：同一线路内**先把纯电话行并入第一个地址行**再跨线路去重（与服务端 `adminLine.deriveOutlets` 的 R-13 写入端口径对齐）；该侧完全无地址时，所有电话合成**唯一一站**（`addr` 为空），不造空地址行。
- **计数**：`fromAddrCount` / `toAddrCount` 只数 `addr` 非空者（不再用 `fromStations.length`）。
- **交互**：电话由「一个胶囊装多号 + ActionSheet 选号」改为**逐号一个胶囊 + 点击直拨**（`onCallOne`），与 F-DETAIL-01 统一。
- **必须重传云函数 `searchCompany`** 才生效（前端 `pages/company-detail` 只是渲染侧配合）。

---

### F-REPORT-01 信息纠错入口 ✅

**描述**：平台数据来自公开渠道（用户确认），**必须给出反馈通道**。v6.0 新增，v6.4 改为**站内表单**。

**v6.4 改版（为什么改）**
- 旧实现是「弹 `showModal` 说明 → `showActionSheet` 二选一（打电话 / 复制微信）」。
  用户要**跳出小程序、切到电话或微信 App** 才能完成反馈，摩擦极大，实际几乎无人反馈；
  且反馈内容全靠客服口头记录，没有结构化留存，无法统计"哪家公司被反复报错"。
- 现改为站内表单页 `pages/report`（F-REPORT-02），写完即提交，数据落 `feedback` 集合。

**入口（3 处，均跳 `pages/report`）**
1. 专线详情页底部「信息有误？点这里报错」（`pages/line-detail`，琥珀色按钮 `pf-report-btn`）
   → `?kind=line&key=<线路 ref>&name=<公司名>`
2. 公司详情页底部同一按钮（`pages/company-detail`）→ `?kind=company&key=<公司名>&name=<公司名>`
3. 「我的」菜单新增「信息纠错」→ **不带参**进入（用于用户不在具体对象页时反馈，如"某某公司已停业"）。
   - 不带参时表单顶部的「你要反馈的信息」提示条**整块隐藏**，不留空条。

**规则**
- 入口文案与样式两个详情页保持一致（`pf-report-btn`，定义在 `app.wxss`）。
- 线路的 `key` 取 `lineKey.lineRef(line)`（`_id` 优先，与 F-DETAIL-01 跳转同口径）。
- 传参**只用于**提示条展示与写入定位字段，**不参与任何权限判断**，缺参不影响页面可用性。
- 联系方式取自 `utils/contact.js`（纠错表单本身不再直接拨号，客服入口仍在「联系客服」页）。

---

### F-REPORT-02 信息纠错表单 ✅ v6.4 新增

**描述**：站内反馈表单，结构完全对照参考图。页面 `pages/report`。

**表单结构（自上而下）**
1. **你要反馈的信息**（琥珀提示条）：标签「你要反馈的信息」+ 被反馈对象名。
   - 对象名由路由参数带入；**为空时整块不渲染**。
2. **问题类型**（必填，单选 chips）：电话有误 / 公司已停业 / 线路已取消 / 信息不完整 / 其他。
   - 再次点击已选项可取消（便于误触后纠正）。
   - **白名单唯一的源在云函数** `reportFeedback.TYPES`；前端只传 `key`，`label` 由服务端反查。
3. **具体说明**（必填）：`textarea`，`maxlength=200`，右下角实时 `N/200` 计数。
4. **上传凭证**（选填，最多 3 张）：虚线「+ 添加」框；已选图可点击预览、右上角 × 删除。
   - 选图用 `wx.chooseMedia`（`sizeType: compressed`）；上传云存储后存 `fileID`。
   - **逐张串行上传**：并发上传在弱网下失败原因难归因，且一次最多 3 张，串行耗时差异可忽略。
   - **部分成功也保留**：已上传成功的图不因后续一张失败而丢弃。
   - 删除时顺手 `cloud.deleteFile`，避免云存储留废图。
5. **联系方式**（选填）：`input`，`maxlength=60`，占位「手机号 / 微信号 / 公司名」。
6. **流程说明条**：「提交后我们会人工核实，核实通过后才会修改信息，不会立即生效。」
7. **固定底栏**「提交反馈」（主色渐变，提交中置灰并显示"提交中..."）。

**校验（前端只为体验，服务端必须再验一次）**
| 项 | 前端 | 服务端（`reportFeedback`） |
|---|---|---|
| 问题类型 | 必选，否则 toast | 必须在 `TYPES` 白名单内，否则 `bad-type` |
| 具体说明 | 必填，`maxlength=200` | 必填，`trim` 后为空拒；**截断到 200** |
| 图片 | 最多 3 张 | 截断到 3 张、去重、`fileID` 裁到 300 字符、非字符串项丢弃 |
| 联系方式 | 选填 | 截断到 60 |

**防重复提交**：`ui.lock(this, 'submit')` + 按钮置灰，避免连点写入多条相同反馈。

**写入 `feedback` 集合**
`{ type, typeLabel, target, targetKey, targetKind, detail, images[], contact, status, month, day, ts, _openid }`
- `status` 初始 `0`（待处理）；后台核实后自行改状态，云函数不改。
- `month`（yyyy-MM）/ `day`（yyyy-MM-dd）按东八区，**与 `stat_events` 同口径**，便于同一维度聚合。
- `_openid` **由服务端显式注入**（云函数写入不自动带），用于识别"同一人反复提交同一条"。

**服务端护栏（`cloudfunctions/reportFeedback`）**
- **类型白名单反查**：不信任前端传来的 `typeLabel`，用服务端映射表反查，防止被篡改后把任意字符串写进后台列表页。
- **字段长度全部服务端再截断**：前端 `maxlength` 可被绕过，不兜住单条反馈可能塞进几 MB 文本。
- **频控**：同 `openid` 单日上限 **10 条**（正常用户一天几条已很宽松；脚本刷量是百/千级）。
  - 计数失败时**保守放行**（宁多记勿漏记），与 `statLog` 同策略——频控是防刷手段，不该因一次查询抖动拦住正常用户。
- **无 `openid` 直接拒**（`no-openid`）：不静默写成匿名反馈，否则后台无法回查也无法频控。
- **自愈建集合**：`createCollection('feedback')`，失败只打日志、缓存清空待下次重试（与 `statLog` 同做法）。
- **异常不抛穿**：写入失败返回 `{ ok:false, reason:'write-failed' }` + 中文 `error`，绝不返回 `undefined`。
- **上传失败提示**：云函数未部署时前端给 `ui.notDeployed('reportFeedback')` 的可操作指引，避免用户误以为是网络问题反复重试。

**回归测试**：`.workbuddy/scripts/test-reportfeedback.js`（**61 断言**）——覆盖白名单、长度截断、图片归一、频控、`openid` 缺失、异常不抛穿共 8 组。
> 该文件**独立复刻**云函数校验规则作为期望值（不从云函数导入常量）——若导入，常量改了测试跟着改，等于没测。改云函数校验规则时必须同步改该文件。

---

### F-AGREE-01 协议与政策 ✅

**描述**：三份法律文本。v6.0 新增**免责声明**为独立第三份文档。

| 文档 | `type` 参数 | 入口 |
|---|---|---|
| 用户服务协议 | `service` | 「我的」菜单 |
| **免责声明** | `disclaimer` | **首页页脚** + 「我的」菜单 |
| 隐私政策 | `privacy` | **首页页脚** + 「我的」菜单 |

**免责声明要点**：① 信息性质（公开渠道整理，不保证准确完整及时）；② 平台角色（仅信息查询，不参与承运、不构成推荐担保）；③ 交易风险自行承担（提示核实资质、自行协商纠纷）；④ 信息纠错（指向 F-REPORT-01 与客服电话）；⑤ 联系方式（法定披露：陈镇）。

> **实现细节**：`onLoad` 用 `AGREEMENTS[options.type] ? options.type : 'service'` 做**查表容错**——
> 旧版是三元 `type === 'privacy' ? 'privacy' : 'service'`，新增 disclaimer 后若不改会把免责声明错落到服务协议。

---

### F-HOME-02 首页公告 ✅

**描述**：首页顶部展示滚动公告栏，点击弹出全文。（v6.0 后位于入口卡下方，有配置时才渲染。）

**规则**
- 读取 `notice` 集合**固定 `_id = 'current'`** 的文档（避免 `limit(1)` 无序命中历史脏数据）。
- 无内容 / 读取失败时**整个公告栏不渲染**，不留空条。
- 点击弹 `showModal` 展示全文（`showCancel: false`）。
- 服务端保存时截断至 **500 字**（公告为跑马灯展示，超长会撑破卡片）。

---

### F-HOME-03 首页广告位 ✅

**描述**：首页展示已售出的 10 个公司卡片式广告位。

**规则**
1. 固定 10 个编号位（`id` 1~10），按 `id` 升序查询。
2. **仅渲染已售出的位**（有 `companyName` 才算已售出）；空位一律不渲染。
   - 合规原因：微信审核会拒绝"页面内含空白广告位、招商广告"。
   - **加载失败时同样不展示任何广告位**（`ads: []`），不降级展示占位。
3. 卡片展示：首字母 Logo（8 色循环配色 `colorIdx`）+ 公司名 + 区域 + 数字（`number`），第三段「查看专线 ›」仅在有 `lineId` 时出现。
4. 点击卡片：若该位绑定了 `lineId`，跳转对应专线详情；无绑定则**无任何响应**。
5. 兼容旧数据：无 `companyName` 时回退读旧字段 `title`。


---

### F-SEARCH-01 专线搜索（直达 + 中转）✅

**描述**：核心功能。输入出发地→目的地，返回匹配的直达专线；无直达时推荐中转方案。

#### 服务端架构（万股级设计）
采用**两级查询**，避免"拉全量进内存过滤"在枢纽城市漏单（旧版单查询上限 1000 条）：

```
① 数据库粗过滤（走复合索引，毫秒级）
   ├ 标准城市字段精确查：fromCity == 标准城市（新数据）
   ├ 旧字段后缀变体 in 查：fromCityName ∈ {济南, 济南市, 济南地区, 济南盟, 济南自治州}（历史兼容）
   └ 辐射区兜底查：fromAreas 正则含 输入token 或 "全境"
② 内存精过滤（候选集仅"该城市的线路"，通常几十~几百条）
   └ 原有模糊匹配能力完全保留：辐射区 / 区县归属 / 别名互通 / 乡镇容错
```

**用户输入 → 标准城市的映射**由 `regionData.getCityLevel()` 完成（区县→上级市，市/省→自身），纯内存查表。

#### 地名匹配规则（`placeMatch.js`，共 5 条）
| # | 规则 | 说明 |
|---|---|---|
| 1 | 归一化 | `norm()` 去后缀，用于城市/行政区归属判定 |
| 2 | 别名表 | 常见地名异写互通 |
| 3 | 辐射区前缀锚定 | "XX全境"等写法的前缀锚定匹配 |
| 4 | 全境归属 | 区县输入归属到上级城市 |
| 5 | 乡镇级容错 | `sameCity()` 支持"羊流镇"="羊流" |

#### 直达匹配逻辑
一条线路被判定为直达需**两头都匹配**：
- **发站匹配**：`sameCity(fromCityName, fromCity)` 或 `sameCity(fromCityName, fromText)` 或 `isParentCity(...)` 或 `sameCity(fromCity, stdFrom)`，或落到辐射区 `contains(fromAreas, ...)`。
- **到站匹配**：同理，另有辐射区 `contains(toAreas, ...)`。

**`transfer` 标记**：当到站仅靠**辐射区**命中（而非城市名命中）时，`transfer = 1`，列表展示"🔄 中转"徽标并渲染三段式路由 `出发地 → 线路到站城市 → 用户目的地`。

#### 中转匹配逻辑（无直达时才执行）
- 前提：起、到两端都能映射到标准城市级别且不相等。
- 思路：查找"出发城市能到"的中转城市 ∩ "能到目的城市"的中转城市。
- 排除：中转城市不等于起点/终点城市；排除同一条线路同时充当两段（虚拟中转无意义）。
- **上限 20 条**（`.slice(0, 20)`）。
- 去重键：`${firstLine._id||id}-${secondLine._id||id}`。

#### 排序与会员置顶
- 直达结果按 `isVip` 降序排序 → **有效会员置顶**。
- `isVip` 在返回前被修正为**有效会员口径**（`isVip=1` 且未过期），过期会员自动降级。

#### 返回口径
```js
{ ok: true, directList: [...], transferList: [] }   // 有直达
{ ok: true, directList: [], transferList: [...] }   // 无直达，给中转
{ ok: false, error: '查询失败' }                     // 异常
```

#### 数据归一（返回前必须做）
- 缺 `id` 的历史数据用 `_id` 兜底（否则用户点开报"链接已失效"）。
- `tags` / `toAreas` 统一转为**数组**（导入数据可能是逗号字符串，直接给 `wx:for` 会逐字渲染）。

**验收标准**
- [ ] 枢纽城市（超 1000 条线路）搜索不漏单
- [ ] 输入区县名（如"历城区"）能命中发站为"济南"的线路
- [ ] 输入"羊流"能命中线路"羊流镇"
- [ ] 无直达时返回中转方案，点击各段可分别进入详情
- [ ] 过期会员不置顶、不显示 VIP 徽标

---

### F-LIST-01 专线列表展示 ✅

**描述**：展示搜索结果，区分直达/中转/加载/失败/为空五种状态。

**状态机（互斥，按优先级）**
| 状态 | 条件 | 表现 |
|---|---|---|
| 加载中 | `loading` | 3 张骨架屏卡片 + "加载中..." |
| 直达结果 | `lineList.length > 0` | 专线卡片列表 |
| 中转方案 | `transferList.length > 0` | 提示 + 中转卡片列表 |
| 查询失败 | `loadError` | ⚠️ 查询失败 + 具体原因 + **重新查询**按钮 |
| 暂无结果 | 以上都不满足 | "暂未找到相关专线" + 引导换城市 |

> **关键设计**：**"查询失败"必须与"没有线路"严格区分**。失败时置 `loadError` 并展示错误态与重试按钮，避免用户误以为平台真的没有这条线路。

**顶部横幅文案**（随状态切换）
- 直达：`共找到 N 条直达专线`
- 中转：`未找到直达，为您推荐 N 条中转方案`
- 失败：`查询失败`
- 空：`暂未找到相关专线`
- 加载：`正在为你查询...`

**专线卡片内容**：线路名 + 徽标组（直达/🔄中转/👑会员/已认证）+ 公司名 + 路由 + 时效 + 价格 + 标签。

**中转卡片内容**：第一段（公司/路由/时效/价格）→ 箭头（"在 XX 中转"）→ 第二段 → 全程路由。

**交互规则**
- 点击直达卡片 / 中转任一段 → 跳转对应专线详情。
- `wx:key` 直达用 `_id`，中转用组合键 `key`。
- 埋点：`onLoad` 且起终点有效时记一次 `search` 事件。
- 分享：卡片分享标题为 `${出发}到${到达}物流专线查询`，落地页为首页。

---

### F-LIST-02 列表筛选（客户端）✅

**描述**：v6.0 新增。承接「查专线」页透传的筛选条件，**在列表页做客户端过滤**，不增加云函数负担。

**筛选维度**
| 参数 | 含义 | 判定依据 |
|---|---|---|
| `direct=1` | 只看直达 | 排除 `transfer` 标记项；**并整段隐藏中转方案区** |
| `daily=1` | 天天发车 | `tags` 或 `lineType` 含「天天发车/每日发车/天天」 |
| `maxDays=N` | N 天内 | 从 `aging` 文案粗解析天数（「1天」「2-3天」「次日达」「隔日」），**有值且超上限才排除** |

> **关键设计：时效缺失不排除。** `aging` 真实填写率仅 **42.5%**，若把"无时效数据"一律排除，会误杀大半线路。
> 只在 `parseDays()` 能解析出天数且超过上限时才过滤（解析失败返回 0 = 视为不限）。

**交互规则**
1. 筛选条仅在**查询结束且确有结果**时出现（空态/失败态不挂无用 chip）。
2. 「只看直达」「天天发车」为勾选 chip，「清除筛选」在任一筛选生效时出现。
3. 筛选在客户端即时生效，无需重新请求云函数。
4. 筛选生效时顶部横幅追加「（已筛选）」；筛选后无结果时文案为「没有符合筛选条件的专线」（与"暂未找到相关专线"区分）。
5. 服务端返回结果同时保留在 `rawDirect` / `rawTransfer`，取消筛选时无需重新请求。

---

### F-DETAIL-01 专线详情 ✅

**描述**：完整展示一条专线，是转化（拨号/导航）的落地页。

**页面结构（v6.1 对齐公司详情，共用 `pf-*` 卡片语言）**

设计依据：对 475 条真实线路统计字段填写率，**页面自上而下按"有值概率 × 对决策的重要性"排序**，让首屏信息尽量不为空。

| 区块 | 数据来源 | 填写率 | 定位 |
|---|---|---|---|
| 头卡 | 公司名 + 路线 + `lineType`/`aging` + 徽章 | 100% | 主视觉，用户确认"是不是这家、到哪" |
| 发站信息 | `fromOutlets` / `fromAddress` / `fromPhone` | 79% / 61% | 转化核心（蓝卡） |
| 到站信息 | `toOutlets` / `toAddress` / `toPhone` | 78% / 43% | 转化核心（橙卡） |
| 三统计 | 发站网点 / 联系电话 / 到站网点 | 100%（派生） | 一眼看清规模 |
| 覆盖区域 | `toAreas` | 74.3% | 回答"能不能送到我这" |
| 运营信息 | `aging` / `priceDesc` / `lineType` | 42-62% | 辅助参考，**仅渲染有值的行**，三项全空则整卡隐藏 |
| 服务优势 | `tags` | 77.7% | 补充卖点 |
| 底部操作栏 | — | — | 分享 + 收藏 + 导航 + 立即拨打 |

> **设计变更说明（v2）**：原设计首屏放「运输时效 / 参考价格 / 运输方式」三统计卡，但统计显示这三项填写率仅 42.5% / 42.7% / 61.7%，导致大量线路首屏出现 `—` 占位；而填写率更高的「覆盖区域」（74.3%）当时**完全没有展示**。v2 因此把运营信息降级为条件渲染的次要区块，并把覆盖区域提为首个内容区块。
>
> **设计变更说明（v6.1）**：三统计卡由「时效/价格/方式」（低填写率，首屏大量 `—`）改为**「发站网点 / 联系电话 / 到站网点」**——这三项由网点数据派生，**必然有值**，彻底消除首屏破折号；原三项仍保留在「运营信息」卡内条件渲染。同时头部由渐变头图改为**白底描边卡**，与公司详情统一（见 R-23）。

**风险提示（供给侧）**：`fromPhone` 仅 61.1%、`toPhone` 仅 43.2%，意味着**约四成线路在详情页无法直接拨号**，与"拿到电话直接联系"的货主核心诉求直接冲突。建议在导入侧提示补齐电话，或评估用 `phone`（25.5%）做兜底的有效性。两端网点均无电话时，页面改为提示「该专线暂未填写发站/到站地址与电话，可先通过下方按钮联系平台客服核实」，不留空白。

**头卡（`pf-head-card`）**
- 公司名 36rpx 主标题（无公司名时回退 `title`）+ 「已核实」绿胶囊 + 「👑 会员企业」金胶囊。
- 元信息行（`·` 分隔，**每项条件渲染**）：出发城市 → 到达城市 · `lineType` · `aging`。
- **路线不再是独立一行**，与元信息合并为一行，节约首屏高度。

**底部操作栏（`ld-bar`，磨砂浮层）**
- 左：**分享 + 收藏**两个轻量文字按钮（`flex-shrink:0`）。
- 右：「导航」（`flex:1`）、「立即拨打」（`flex:1.5`，主色渐变 + 投影）。
- 分享 = `<button open-type="share">`（转发给同行的高频动作）；收藏走 `storage` + `_favBusy` 锁。

**正文区块**
- 「覆盖区域」：`toAreas` 渲染为绿色胶囊（`.ld-chip-green`）。
- 「运营信息」：标签-值竖排（`.ld-spec-row`），**每行独立 `wx:if`**；`hasSpecs` 为三项的逻辑或，全空整卡隐藏。
- 「服务优势」：`tags` 渲染为中性胶囊（`.ld-chip`）。
- 「服务信息」区块标题仅在**至少一个子卡会渲染**时才出现（避免孤零零一个标题）。
- 发站/到站卡右上角提供「复制地址」文字按钮（`wx:if` 有地址才出现）。


**加载流程**
1. `lineKey.parseLineKey(options.lineId)` 解析键 → 参数非法则直接展示"链接已失效"。
2. 按 `lineKey.lineWhere()` 查询（数字键按 `id`，字符串键按 `_id`）。
3. 未找到 → "未找到该专线，可能已下架"。
4. **`status === 0`（已下架）→ "该专线已下架"**，不再展示。防止停投/违规线路通过旧分享链接、收藏入口继续被访问。
5. 数据归一（顺序不可颠倒）：
   - `lineInfo.id = lineInfo.id || lineInfo._id`（收藏/分享链路需要稳定唯一键）
   - `lineInfo.isVip = vip.effectiveVip(lineInfo)`（过期会员自动降级）
   - `tags` / `toAreas` 转数组
   - **`outlets.deriveLegacy(lineInfo, 'from'/'to')`**：把字符串网点归一为数组并反填空的老字段（`fromAddress`/`fromPhone`）——修复旧导入数据"地址与联系"显示"未填写"
6. 并行处理：收藏状态（不阻塞正文，先渲染再审回填星标）→ 埋点 `view` → VIP 动效降频定时器（5 秒后 `reduceAnimation`，减轻低端机渲染压力）。

**网点展示（核心）**
- 分组来源优先级：
  1. **有 `fromOutlets`/`toOutlets`** → 按写入顺序精确配对，**一个网点可挂多个电话**；
  2. 无网点字段 → 按 `地址[n] ↔ 电话[n]` **下标对位**。
- **★ v6.3 分组规则**（`buildOutlets`，详见 R-13）：**纯电话行不再各自成组**，
  统一并入第一个地址组；该侧完全无地址时合并为唯一一组（`addr` 为空，不显示"地址未填写"）。
  修复前「1 地址 + N 纯电话」会渲染成 N+1 个网点，其中 N 个是"地址未填写"的假网点。
- 展示结构：编号 + 地址（可为空，为空时**整行不渲染**）→ 电话胶囊**横排、自动换行**（flex-wrap）。
- 拆号规则：电话按 `[,，、;；/\s]+` 拆分 → 去重。**v6.3 起不再排序**（原 `.sort()` 会打乱录入顺序，第 1 个常是主号）。
- 拆址规则：地址按 `[\r\n;；|｜]+` 拆分（**逗号不拆**——地址内部常含逗号）→ 去重。**v6.3 起不再排序**（原 `.sort()` 会打乱主发货点顺序）。
- 标题用「发站信息 / 到站信息」，配编号徽标（`pf-st-index`）。

**操作能力**
| 操作 | 规则 |
|---|---|
| 一键拨号（底部） | 单号直接拨；2~6 号弹 `ActionSheet` 选择；**>6 号**引导"请点号码直拨"（ActionSheet `itemList` 上限 6） |
| 点单个号码直拨 | 号码胶囊可点击直接拨打，绕过选择面板 |
| 复制地址 | 单条复制（点地址文本）；「复制地址」（卡右上）多地址以换行拼接，toast 提示已复制 N 个地址 |
| 地图导航 | **双端导航**：发/到货点都有坐标 → ActionSheet 选择导航目标；仅一端有 → 直达；都无 → 降级为"复制地址"；再无 → 提示未设置定位 |
| 收藏 / 取消收藏 | 走 `storage`，`_favBusy` 锁防连点；入口在底部操作栏左侧 |
| 分享 | 标题 `${出发}到${到达}物流专线 - ${线路名}`，路径带 `lineId`；入口在底部操作栏左侧 |

> **v6.1 变更**：原「网点右侧拨号按钮」（`onCallGroup` + `data-index`）已移除——每个电话号码本身就是可点直拨的胶囊，再放一个组级按钮是冗余交互。对应的 `onCallGroup` 处理函数已一并删除（静态体检曾报 P0 孤儿绑定，见 §8.3）。
>
> **v6.3 变更（用户报障）**：网络分组改为「纯电话行并入第一个地址组」（见 R-13），
> 取消电话/地址排序，统计卡由 `fromPairs.length` 改为 **`fromAddrCount`（只数有地址的组）**，
> 标签由「发站网点 / 到站网点」改为 **「发站地址 / 到站地址」**（口径与展示一致）。
> 修复前 1 个地址 + 3 个号码会显示成"3 个网点"，数字与内容都对不上。

**错误态**：`loadError` 时提供「重新加载」与「返回」；`retryLoad` 复用已解析的键，无键时兜底 `switchTab` 回首页。

---

### F-FAV-01 收藏 ✅

**描述**：云端收藏专线，跨设备同步。

**规则**
- 收藏内容为**专线快照**（`line` 字段）+ `lineId` + `createTime`。
- **收藏键以业务 `id` 为准**（`line.id || line._id`）。
  > 历史收藏数据存的也是 `id`，改口径会导致老收藏全部消失。
- **必须循环分页拉全**：小程序端 `collection.get()` 单次默认仅返回 20 条，不分页会导致第 21 条起静默丢失（确定性数据丢失 Bug）。上限 1000 条。
- **读取时修正 VIP 口径**：快照里的 `isVip` 可能已过期，读出时统一按 `effectiveVip` 修正。
- 过滤脏数据：`line` 快照缺失的收藏会产出 `undefined`，必须过滤（否则渲染空白卡片 + 跳转报错）。
- **幂等新增**：先按 `lineId` 查重；并发场景下若命中唯一索引重复键错误（`duplicate/unique/E11000`），按**成功**处理，不向用户报错。
- **删除时全清**：历史脏数据可能同线路收藏了多条，需全部清理（只删第一条会导致"取消收藏后列表里还在"）。
- 错误态：`getFavorites` 提供 `onError` 回调，页面据此区分「真的没有收藏」与「读取失败」（`loadError`），避免丢过收藏的用户误以为收藏没了。

---

### F-USER-01 我的 ✅

**入口**：v6.0 起**不再是 tabBar 页**，从「查公司」页头图右上角「我的」按钮进入（`navigateTo`）。

**内容**
1. **查询历史**（本地）：最新 20 条；按"出发→到达"城市名去重（同一路线再查则移除旧记录并置顶）；时间格式统一 `MM-DD HH:mm`；支持单条重查与一键清空（二次确认）。
2. **管理后台入口**：仅当 openid 在前端白名单 `config/admin.js` 中才显示。
   > 该文件**只控制入口显隐，不含任何写权限**；真实权限以云函数侧校验为准。
3. **功能入口**：收藏、联系客服、用户服务协议、**免责声明**、隐私政策。

---

### F-CONTACT-01 联系客服 ✅

**入口**：在线客服会话（小程序原生 `open-type="contact"`）、客服电话（点击直拨）、微信号（一键复制 + 引导添加）、二维码（长按识别/保存）。
**规则**：`utils/contact.js` 中留空的项**入口自动隐藏**，绝不出现假号码。

### F-FAV-01 收藏 ✅

**入口**：v6.0 起**不再是 tabBar 页**，从「查专线」页头图右上角「收藏」按钮进入（`navigateTo`）。
> ⚠️ 代码注意：`pages/user/index.js` 的 `goToFavorite` 必须用 `wx.navigateTo`，**不可用 `switchTab`**（已非 tab 页，`switchTab` 会失败）。

---

## 4. 功能需求 — 管理端

> 全部位于 `pages/admin` 分包，入口仅对白名单 openid 可见。

### 4.1 权限模型（关键）

```
数据库 app_config 集合 → doc 'admins' → { openids: [...] }
        ↑ 唯一权威来源（云函数侧校验）
        ↓ 实例内缓存 5 分钟
   各管理云函数 getAdmins()
        ↓ 集合不存在时
   FALLBACK_ADMINS 兜底（硬编码）→ 并「自愈」把兜底名单写入数据库
```

- **变更管理员只需改数据库文档，无需重新部署云函数。**
- 前端 `config/admin.js` 的白名单仅用于显示入口，**必须与库中名单保持一致**（否则入口显隐与实际权限不符）。
- 所有管理员云函数的第一道逻辑：`if (!admins.includes(OPENID)) return { error: '无权限操作' }`。

### F-ADMIN-01 管理看板 ✅
展示**专线总数**（实时 `count`）。
> 统计失败必须提示，**不能静默显示 0**——否则管理员会误以为库是空的。

### F-ADMIN-02 专线列表 ✅
| 能力 | 规则 |
|---|---|
| 分页加载 | 每页 50 条，滚动到底自动加载下一页（`loadedCount`/`hasMore`）；页码必须 `+1` 递增，否则反复拉第 1 页导致列表重复 |
| 服务端搜索 | 关键词对 `title`/`companyName`/`fromCityName`/`toCityName` 正则匹配，**防抖 400ms** |
| VIP 状态 | 每条展示 `vipStatusText`：未开通 / 会员至 yyyy-MM-DD / 已过期 yyyy-MM-DD |
| 新增 | 跳编辑页（无参数 = 新增） |
| 编辑 | 跳编辑页带线路键（`_id` 优先，缺 `id` 的老数据同样可编辑） |
| 上下架 | `toggle`，带防连点锁（连点会把状态翻两次回到原值） |
| 删除 | 二次确认（明示不可恢复） |
| 导出 | 已加载线路 → JSON 文件 → `wx.shareFileMessage` 转发；不支持时降级复制到剪贴板 |
| 导入 | 从聊天选 `.json` → 解析 → 二次确认 → **分批 200 条**上传（防请求体超限/超时），实时进度 |

**导入校验**：仅保留 `title` + `fromCityName` + `toCityName` 齐全的记录；导入一律作为**全新记录**（忽略文件中的 `id`/`_id` 并重新生成）——保留旧 id 会导致重复导入产生同 id 多记录，而详情/编辑页取 `res.data[0]` 可能命中错误记录。

**导出实现约束**：`shareFileMessage` 必须在用户 TAP 手势的**同步调用栈**内触发，因此写文件用 `writeFileSync`（异步 `writeFile` 的 success 回调再 share 会丢失手势导致 fail）。

### F-ADMIN-03 专线编辑 ✅

**表单字段**：专线名、公司名、出发/目的城市、地址、到货辐射区、坐标、时效、价格描述、类型、发站/到站电话、标签、VIP 到期、认证、上下架。

**关键规则**
1. **城市双通道**：级联选择 + **手输 input**。手输是必须的——乡镇级地名（如"羊流镇"）不在级联数据里。
2. **标题自动预填**：新增模式下城市选齐且标题为空时填 `${出发}-${到达} 专线`；**已手填则不覆盖**。
3. **线路类型选项**：内置「直达/中转」，老数据的自定义值（如"专线"）追加为第三项，避免显示错位。
4. **VIP 管理**：用**到期时间**管理，替代旧的 `isVip` 裸开关。
   - 续费/开通按钮（30 / 365 天）：在 `max(现有到期时间, 当前时间)` 基础上顺延；
   - 停用：清零到期时间；
   - **保存时 `isVip` 由到期时间派生**（`vipExpireAt > now ? 1 : 0`）——收费事实来源是 `vipExpireAt`。
5. **时效规范化**：纯数字或 `2-3`/`2~3`/`2至3` 格式自动补"天"。
6. **电话校验**：按分隔符拆段逐段校验（允许数字与 `-`，5~20 位）；**发站/到站电话至少填一个**。
7. **必填校验**：专线名 + 出发城市 + 目的城市。
8. **保存防连点**：新增时 `id = Date.now()`，连点会创建两条重复专线，必须加锁；校验失败要**解锁**让用户改完再点，成功路径不解锁（等页面返回）。
9. **地图选点**：发货点选点自动填入发货地址并记录选点名；到货点同理（供收货方导航）。
10. **加载时 `deriveLegacy` 反填**：旧数据网点存成字符串、老字段为空，直接回显会让管理员误以为没填，保存时还会用空值冲掉网点数据。

**服务端字段白名单**（`FIELD_WHITELIST`）：防止误传/多余字段污染文档；数组字段（`toAreas`/`tags`）写入端统一归一为数组；数值字段强制 `Number()`。

### F-ADMIN-04 VIP 批量按公司开通/续费 ✅
- 入参：`companyName` + `days`（天），`clear=true` 表示停用。
- 续费基准：`max(现有 vipExpireAt, 当前时间) + days`。
- **必须分页循环**（每页 1000，上限 20 页 = 2 万条）。
  > 原来单次 `limit(1000)` 就结束：公司线路超 1000 条时只有前 1000 条被续费，其余静默漏掉（用户续了费却仍是过期会员）。
- 前提：线路的 `companyName` 字段必须准确（导入时建议填写）。

### F-ADMIN-05 广告管理 ✅
- 展示 10 个广告位骨架（1~10），每个显示当前公司名。
- **区分"加载失败"与"真空位"**：失败时占位名显示"加载失败"，否则管理员会把网络失败误判成"10 个位都空着"。
- 编辑：公司名 / 区域 / 数字 / 关联 `lineId`。
- **服务端校验**：`id` 必须为 1~10 的整数（否则会新增无 `id` 的脏 doc，首页永远匹配不到 = 存了不显示）。
- `lineId` 支持两种键：数字业务 id（新数据）与 `_id` 字符串（缺 id 的历史线路）。**不能用 `Number()` 强转**，否则 `_id` 变 `NaN→0`，广告跳转失效。

### F-ADMIN-06 公告管理 ✅
编辑首页公告文本，保存走 `adminNotice`（固定 `_id = 'current'` 幂等 upsert，服务端截断 500 字）。

### F-ADMIN-07 月度统计 ✅
| 指标 | 口径 |
|---|---|
| 活跃用户数 | 按 `_openid` 去重；优先用聚合 `$count` 阶段（**无 1000 条限制**，精确值）；不支持时回退 `group+limit`，此时置 `usersCapped` 标记，前端展示 `1000+` 并说明——**不把截断值当精确值解读** |
| 搜索总次数 | `type='search'` 计数 |
| 浏览总次数 | `type='view'` 计数 |
| 用户榜 | 按操作总数降序，**取前 50 名**；含 search/view 分别计数与最后活跃时间 |

- 月份切换：默认当月，可向前无限翻、向后最多到当月。
- openid **脱敏展示**（前 6 + `****` + 后 4）。
- 用途：评估用户使用强度与专线覆盖是否充分（**不再用于发奖**）。

---

## 5. 统计与埋点

### R-01 埋点口径 ✅
- **PV 口径，不去重**。事件类型：`search`（key = "出发 → 到达"）、`view`（key = 专线 id）。
- 时间口径：`month`(yyyy-MM) / `day`(yyyy-MM-dd) **均按东八区**（云函数默认 UTC，需 +8 小时偏移）。
- `_openid` 由**服务端显式注入**（云函数写入不会自动带 `_openid`，而统计按该字段聚合，口径必须一致）。

### R-02 防刷量频控 ✅
- 埋点**必须经 `statLog` 云函数**写入，不可由小程序端直连。
  > 原因：端上直写时，任何人可用脚本高频写 `search`/`view` 污染统计数据（曾用于冲月度活跃榜）。
- 频控阈值：**同一用户同一自然日最多 100 条**事件（search/view 合计），超出静默丢弃。
  > 阈值取值理由：一次搜索 = 1 条 search + 用户点开详情产生若干条 view，正常重度用户一天几十条；曾试 30 会提前封顶重度用户 = 变相惩罚活跃用户导致榜单失真；100 能覆盖真实强度且能拦住千/万级脚本。
- 频控计数查询失败时**保守放行**（宁多记勿漏记）。

### R-03 统计失败不影响主流程 ✅
所有埋点为 **fire-and-forget**：失败只打日志，绝不阻塞用户操作。

---

## 6. 数据一致性规则（关键护栏，改代码前必读）

> 这一节记录的是**踩过坑之后加上的护栏**。违反任一规则都可能造成静默数据丢失或用户可见的严重 Bug。

### R-10 线路主键双键制 ⚠️
- `_id`（数据库主键，必有）与 `id`（业务字段，毫秒时间戳，旧数据可能缺失）并存。
- **凡"按线路定位"一律走 `utils/lineKey.js`**：`parseLineKey()` / `lineWhere()` / `lineRef()`。
- **禁止再写 `where({ id: xxx })`**。
- 自愈机制：
  - `searchLine.normalizeLine` 给返回项补 `id = _id`；
  - `adminLine` 列第一页时跑 `healMissingIds()` 自愈补 `id`（手动触发 `action:'healIds'`）。
- 列表 `wx:key` 用 `_id`（`id` 可能缺失导致 key 冲突）。
- 收藏键以业务 `id` 为准（`line.id || line._id`）。

### R-11 城市索引字段派生 ⚠️
- `fromCity`/`toCity` 由服务端 `normCity()` 派生（去"市/区/县/省/自治州/盟/地区"后缀）。
- **仅在本次提交带了城市名时才派生**。局部更新（只改标题/状态）时若无条件赋 `normCity(undefined) === ''`，会把库里已有的 `fromCity`/`toCity` 清空，**该线路随即从所有搜索中消失**。
- `searchLine` 查询侧同口径归一，**两侧必须一致**。

### R-12 地名匹配护栏 ⚠️
- 匹配规则集中在 `cloudfunctions/searchLine/placeMatch.js`。
- 城市字段比对**必须用 `sameCity()`**（含镇/乡/街道后缀容错），不要退回 `norm()` 严格相等。
- **禁止给 `norm()` 增加"镇/乡"后缀剥离**：会把「景德镇市」削成「景德」，`regionData` 的 `PREFACE_KEYS`/`isParentCity` 立刻断链。regionData 归属判定一律走 `norm`。
- **单字地名只做精确相等，不做包含**（防「陵」误命中「乐陵」）。

### R-13 网点 ↔ 地址/电话 双向一致 ⚠️
- 服务端 `adminLine.deriveOutlets` 两道护栏：
  1. **三者都未提交时不动网点字段**（防部分字段更新把已存在的网点清空）；
  2. **单地址多电话保持 1 网点挂多号**（不按下标对位拆散成一堆"纯电话"组）。
- 读端统一走 `utils/outlets.js` 的 `parseOutlets/deriveLegacy`，**口径须与服务端一致**。新增消费网点/地址/电话的页面必须先过 `deriveLegacy`。
- 解析分隔符：**行分隔用换行/分号，不用逗号**（地址内部常含逗号）。

#### ★ 读端也必须遵守"单地址多电话 = 1 组"（v6.2 修复）
写入端的护栏（第 2 条）只约束了服务端落库，**读端曾按行逐条成组**，导致直灌数据的
「1 个地址行 + N 个纯电话行」（形态②，真实数据的主要形态）被渲染成 N+1 个网点，
其中 N 个显示「地址未填写」——用户看到的是"凭空多出几个没填地址的发站"，
与 R-13 第 2 条**直接冲突**。

**修复后的统一分组规则**（`pages/line-detail` 的 `buildOutlets` + `searchCompany` 的 `collectStations`，两处口径必须一致）：

| 输入 | 分组结果 |
|---|---|
| 1 地址行 + N 纯电话行 | **1 组**：该地址挂 N 个号码 |
| M 个地址行（各带电话） | **M 组**：地址与电话严格一一对应 |
| M 个地址 + 尾随纯电话 | **M 组**：尾随号码并入**第一个**地址组 |
| 只有电话、没有地址 | **1 组**：`addr` 为空（前端**不再渲染**「地址未填写」） |

- **纯电话行不再各自成组**；把无地址号码拍平后并入第一个地址组。
- 合并进第一个组时**必须再对组内已有号码去重**——直灌数据里同一号码可能既写在地址行内、
  又单占一行（"一个号写了两遍"），不去重会重复展示（`.workbuddy/scripts/test-outlet-pairing.js` 用例 8 抓出过）。
- **前端不再渲染「地址未填写」**：不会再有"有电话但没地址"的组（该侧完全无地址时是唯一一组，
  只显示电话，不显示地址行）。`app.wxss` 的 `.pf-st-addr-empty` 已随之删除。
- **计数口径同步修正**：统计卡的"发站地址 / 到站地址"只数**有地址的组**（`fromAddrCount`），
  公司详情的"共 N 个"同理——否则 1 个地址 + 3 个电话会显示成 3 或 4，数字虚高。
- **不再排序**（v6.2）：`splitPhones` / `splitAddrs` 原先各自 `.sort()`，会打乱运营录入的
  原始顺序（第 1 个常是主号/主发货点）。现一律**保持录入顺序**，仅去重。
- **回归测试**：`.workbuddy/scripts/test-outlet-pairing.js`（9 组共 30 断言，覆盖上述四种输入
  与去重、数量不等、idx 连续性）。改 `buildOutlets` 后**必须同步改该测试里的副本**并跑通。

### R-14 数组字段形态统一 ⚠️
- `tags` / `toAreas` 入库有三条路径，形态不统一：① 新版 `adminLine` → 数组；② 旧版批量导入/控制台直灌 → 逗号字符串；③ 手工编辑 → 可能有值。
- **写入端统一归一为数组**（`toArr`），读端（`searchLine.normalizeLine` / 详情页 / 列表页）也各自归一。
- 一次性迁移：`migrateArrayFields()` 分批扫描，用 `app_config/migrated_arrays_v1` 记录 `cursor` 与 `done` 标记，**翻完一遍后不再扫描**。

### R-15 VIP 有效性口径 ⚠️
- **有效会员 = `isVip === 1` 且（`vipExpireAt` 缺失[兼容旧数据] 或 大于当前时间）**。
- 前端 `utils/vip.js` 与服务端 `searchLine.effectiveVip` **两处实现必须一致**。
- **`searchCompany.effectiveVip` 是第三处实现**（v6.0 新增），同样必须遵循此口径。
- 直接用库的场景（收藏快照、详情页、公司聚合）必须调 `applyEffectiveVip` / `effectiveVip` 修正。
- 旧数据自愈：`adminLine` 列表首屏把 `isVip=1` 且无 `vipExpireAt` 的记录补为当前时间 +1 年；导入时同理。

### R-16 地区数据单一源（展示 ↔ 匹配一致）⚠️ v6.0 新增
- **`cloudfunctions/searchLine/regionData.js` 是地区数据的唯一源**，已导出 `MUNICIPALITIES` / `PROVINCE_CITIES` / `CITY_COUNTIES` 三张原始表。
- 小程序端不能 require 云函数目录 → 由 `.workbuddy/scripts/sync-region-data.js` **自动生成** `utils/region-data.js`（文件头标【自动生成 · 请勿手动编辑】）。
- **改动流程：改云函数那份 → 跑 `node .workbuddy/scripts/sync-region-data.js` → 该脚本内置自检**（南充样本必须解析出「高坪区」，否则退出码 1）。
- **禁止**前端手抄一份地区表：一旦与搜索匹配用的表漂移，会出现"界面上能选的地区搜不到线路"。
- **展示名 ≠ 查询名**：区划表存规范短名（「顺庆」），展示需补后缀（「顺庆区」）。`displayName()` 用**显式映射表 `DISPLAY_SUFFIX`**，**不可用正则统一加后缀**——「乌鲁木齐县」的「县」是名称本身的一部分，正则剥离/追加都会错。
- **禁止**给 `norm()` 增加"镇/乡"后缀剥离（见 R-12）。
- 同名多地（市中 / 城区 / 东区 / 西区 / 海南 / 朝阳 / 吉林）由弹层**逐级下钻的父级上下文**消歧，不要按名字全局匹配。

### R-17 公司维度实时聚合（不建表）⚠️ v6.0 新增
- **不建独立 `companies` 集合**。公司所有展示字段（认证 / VIP 数 / 覆盖城市 / 规模 / 线路清单）均由**在营线路实时聚合**得出，避免双份数据不一致（改了线路忘了改公司表）。
- 数据可行性依据：`companyName_1` 单字段索引**已存在**，且 `companyName` 是导入必填字段（实测 100% 有值）。
- **`status === 0`（已下架）必须排除**在统计之外，否则已停运线路会被算进"覆盖线路"。
- 口径必须对齐既有护栏：VIP 走 R-15；城市名走 `normCity` 去后缀（与 R-11 同口径）；线路引用走 `_id` 优先（与 R-10、`lineKey.lineRef` 一致）。
- **中转语义在 `lineType`（"直达"/"中转"）里，数据中不存在独立的 `transfer` 布尔字段** → 判中转必须同时认 `transfer` 与 `lineType`。
- **网点三形态必须在服务端归一**（口径对齐 `utils/outlets.js` 的 `parseOutlets`）：① 对象数组 `[{addr, phone}]`；② **整段字符串** `"地址 | 电话1,电话2"`（可能含 `\n` 多行，真实数据的主要形态）；③ 字符串数组。**只处理其中一种会漏站**。
- 单公司站点展示上限 20 个（防异常数据撑爆页面）。

### R-18 元信息的"宁缺毋滥"（展示层不得造数据）⚠️ v6.1 新增
- **原则**：详情页头部元信息（`updateText` / `viewCount`）**任何一项无可靠数据来源就不显示**，绝不显示占位符、不编造、不用近似值糊弄。
- **`updateText` 的唯一来源是 `_id` 的 ObjectId 时间戳**（前 4 字节 = 创建时间秒），因为：
  1. 线路文档**没有任何时间字段**（实测 475 条导入数据 0 命中；`adminLine.pickFields` 的 `FIELD_WHITELIST` 亦不含 `createTime`/`updateTime`）；
  2. 云函数 `add` 写入的文档其 `_id` 必然符合 ObjectId 规范。
  - **三道校验缺一不可**：① `_id` 必须是 24 位十六进制；② 时间戳须落在 2015-01-01 ~ 2100-01-01（防手工造 id 得出荒谬时间）；③ 相对时间**超过 90 天返回空串**（不显示"286天前"这类无意义信息）。
- **`viewCount` 是可失败的可选统计，不得影响主流程**：
  - **必须 try/catch 吞掉所有异常**并返回 0（集合不存在、超时、权限错误均如此）。
  - **线路 id 上限 30 个**（`VIEW_ID_LIMIT`），超出直接放弃统计。
    > 原因：`stat_events` 仅有 `month_openid` / `day_openid` / `month_type` 三个索引，**没有 `type+key` 索引**；`_.in(大数组)` 会退化为扫描，会拖慢整个详情页响应。
  - **回归护栏**：`test-searchcompany.js` 必须包含"统计接口抛错时 viewCount 归 0，且详情主数据完整"的断言。
- **前端一律条件渲染**：元信息行每一项独立 `wx:if`，无值时不渲染该项**及其前面的分隔符**（否则会出现孤立的 `·`）。

---

## 7. 非功能需求

### R-20 性能与规模
- 目标规模：**万股级线路数据**。
- 搜索必须走**数据库索引**（`fromCity`、`fromCity+status` 等复合索引，见 `cloudfunctions/createIndexes/README.md`），**严禁"拉全量进内存过滤"**（旧版单查询 1000 条上限，枢纽城市必然漏单）。
- 列表与分页上限：云函数单次 `get` 上限 1000 条；管理端列表每页上限 100 条。
- **索引必须先建再导入**，否则搜索不走索引。
- VIP 动效在详情页展示 5 秒后降频（`reduceAnimation`）。

### R-21 健壮性
- **自愈机制**：集合不存在时自动创建（`createCollection`，带 Promise 缓存避免重复网络往返）；`status` 缺失补 1；`id` 缺失补 id；数组字段形态迁移。
- **区分"失败"与"空"**：列表页 `loadError`、收藏页 `loadError`、广告页"加载失败"占位、看板统计失败提示——一律不得静默降级为空。
- **防连点**：跳转、保存、核销、上下架、导出等操作统一用 `ui.lock/unlock`。
- **全局错误上报**：`app.js` 监听 `onError` / `onUnhandledRejection`，写入 `app_errors` 集合（`msg` 截断 1000 字），供线上排障。
- **降级策略**：导出失败降级剪贴板；导航无坐标降级复制地址；toast 长文案降级 modal。

### R-22 合规
- 见 §1.5。
- 首页不渲染空白广告位。
- 不收集个人信息；仅存 openid、收藏内容、本地查询历史。

### R-23 UI 规范（v6.2 · 两套卡片语言 + 全站无毛玻璃 + 天空渐变）
- **设计令牌唯一来源：`app.wxss` 的 `page{}` 规则**。WXSS 一律 `var(--令牌名)`，**禁止硬编码十六进制色**。
- **令牌分三组**：
  1. **墨色/品牌系（Notion 基调）**：`--brand` `#2383e2`、`--ink-title` `#191918`、`--ink-body` `#37352f`、`--ink-muted` `#73726e`、`--ink-disabled`、`--brand-light`、`--border` `rgba(60,60,60,.08)`。
  2. **卡片底色系（原「`--glass` 磨砂系」）**：`--glass` `#ffffff` / `--glass-strong` `#ffffff` / `--glass-soft` `rgba(255,255,255,.96)` / `--glass-border` `#ffffff`；工具类 `.glass` / `.glass-strong` / `.glass-soft`。**`--glass-blur` / `--glass-saturate` 已废弃**（v6.2 起不再使用，仅为兼容保留定义）。
  3. **`--sky-*` 天气色板**：`--sky-blue/teal/amber/violet/rose/green`（浅渐变底）+ `--sky-*-ink`（对应深色文字）；工具类 `.sky-*`。

#### ★ v6.2：全站已移除毛玻璃（`backdrop-filter` 全删）
- **范围**：19 个文件、86 处（含管理端）。执行脚本 `.workbuddy/scripts/remove-glass.js`（幂等、带 `--dry` 试运行与残留自检）。
- **为什么删**：① 模糊半径在小程序真机上开销大，长列表滚动掉帧；② 半透明白底会把页面天空渐变透到正文下，**削弱正文可读性**——详情页信息密度最高、阅读时间最长，收益最差。
- **★ 连带必改（关键）**：只删模糊、保留 `.88` 半透明，卡片会变成"透着背景渐变的淡灰块"，**比原来更脏**。删模糊必须**同时把半透明白底提纯为实心白/近白**。
- **★ 迁移脚本的踩坑（必须记住）**：提纯**只能作用于`底色类`属性**（`background*` / `border*` / `box-shadow`）。首版脚本无差别替换，把 `color: rgba(255,255,255,.96)` 也提纯成近白——那是**深色渐变头图上故意用的白色文字/占位符/图标**，与白底放在一起会"白字白底"直接消失。脚本现已改为**逐行判定属性名，`color` 一律跳过**。
- **`.glass*` 类名保留**以兼容既有引用，但已不再产生任何模糊效果。
- **不再有"页面根节点必须 `background: transparent`"的强制约束**（该约束是 `backdrop-filter` 只模糊父级背景所必需的）。但**仍保留** `background: transparent` 写法——页面天空渐变画在 `page` 元素上，根节点透明才能看见它。

#### ★ 两套卡片语言必须分清用途（v6.1 确立，v6.2 仍成立）
| | **浏览型页面** | **档案型页面** |
|---|---|---|
| 页面 | 首页 / 查专线 / 查列表 / 查公司 / 收藏 / 我的 | **专线详情 / 公司详情** |
| 类前缀 | 各页自有 + `.card` / `.glass*` | **`pf-*`（profile，全局）** |
| 卡片底 | 半透明白 → **v6.2 起实心白/近白** | **纯白 `#ffffff` + 细描边 `var(--border)`** |
| 圆角 | 24-28rpx | 20rpx |
| 投影 | 蓝色调柔和多层投影 | 极浅 `0 1rpx 3rpx` |
| 首屏 | **渐变头图**（天空蓝调 / 收藏紫调）+ 内容负 margin 上叠 | **白底描边头卡**打头，无渐变块 |

> **v6.2 后两套语言的差异退化为**：**有无渐变头图** + **圆角/投影强度** + **有无细描边**。卡片底色的差异消失（都变实心白）——毛玻璃曾是最主要的区分手段，移除后档案页靠"白卡打头、无渐变、细描边、弱投影"仍然保持"资料页"气质。
> **`pf-*` 必须放 `app.wxss`**：两个详情页各写一份必然漂移（本项目曾因多页各写一份而出现值漂移）。**页面不得覆盖 `pf-*` 的色值**，只允许加布局微调。

- **调参约定**：卡片"发灰/发脏" → 检查底色是否被写成半透明白（应为实心白或 `≥.96`）；渐变偏淡 → 加深 `page` 底色与头图 `linear-gradient`。
- **头图渐变色值在多个页面各自硬编码**（首页/列表/我的为蓝调 4 段，收藏为公司紫调 4 段，管理端头图同蓝调）——改时需一起改，**尚未抽成令牌，是已知的可优化点**。（档案页已不用渐变，不受影响。）
- `--ink-disabled` 仅用于禁用态，禁止用于正文；`--brand-light` 只用于渐变。
- **JS / `app.json` 不支持 CSS 变量**，需手写字面量并手动同步：`app.json` 的 `window.navigationBarBackgroundColor`（`#4f92dd`）/ `navigationBarTextStyle`（`white`）/ `backgroundColor`（`#e8f0fa`）、tabBar `selectedColor`（`#2383e2`）/ `color`（`#8a93a3`）/ `backgroundColor`（`#ffffff`）。
- **VIP 金色分场景**：**渐变头上**用浅金 `#ffe9a8` + 柔和光晕；**白底上**用深金 `#a16207`（`pf-vip` 用 `#9a6700`）。**禁止流光/发光/呼吸/扫光动画**（与整体气质冲突，曾试做被否）。
- **渐变头图上的白色文字是刻意的**（标题/副标题/搜索框内文字/占位符）——**不要**被"提纯白底"的脚本误伤；深色文字仅用于**白底**之上。
- **「已核实」统一为绿底描边胶囊**（`--sky-green-ink` + `rgba(233,247,236,.9)` 底 + `rgba(63,122,58,.22)` 边），列表卡与详情头卡一致——原列表卡用的是蓝底，v6.1 已统一。
- **目录**：`components/` 放跨页复用组件（当前 1 个：`region-picker`）。组件一律 `options:{addGlobalClass:true}`，样式用页面令牌。
- 详见 `docs/设计令牌.md`；批量收敛用 `.workbuddy/scripts/apply-design-tokens.js`。

### R-24 部署
- 云函数改动后需在开发者工具中**重新上传部署**（右键 → "上传并部署：云端安装依赖"）。
- 环境 ID：`cloud1-d0ge42roj61603242`。
- **上线后纪律**：任何代码/配置改动都需**重新提交审核**；不得在生产配置上做"顺手优化"（如 `libVersion` 保持 `trial` 不动）。

---

## 8. 已知缺口与后续规划

> 以下为**当前实现的客观缺口**，建议作为下一阶段迭代输入。

### 8.1 会员体系 🟡
| 缺口 | 现状 | 建议 |
|---|---|---|
| 无自助购买/续费 | VIP 全靠管理员后台手动开通 | 接入微信支付，用户端自助购买（需先有商品/订单模型） |
| 无订单与流水 | `vipExpireAt` 是唯一事实来源，无购买记录 | 新增 `vip_orders` 集合，记录支付流水、开票信息 |
| 无到期提醒 | 到期后静默降级 | 到期前 N 天站内提醒 / 订阅消息 |
| ~~无聚合查询~~ | 🟡 **用户端已实现（v6.0）**：新增 `searchCompany` 云函数 + 「查公司」tab 页 + 公司详情页，可按公司名实时聚合线路数、会员数、认证、覆盖城市、发/到站网点与线路清单。**管理端**仍只有"批量开通"写操作，缺公司维度视图 | 管理端补公司维度视图（该公司线路数、会员状态、到期分布） |
| 无分级 | 只有"会员/非会员"二元 | 如需多档权益（如置顶强度、展示位），需扩展等级字段 |

### 8.2 数据能力 🟡
- 导出仅覆盖**已加载部分**（受云函数响应大小限制），万股级下无法全量导出 → 建议加服务端流式导出或分页导出。
- 统计聚合上限：活跃用户数最精确到 `$count`，但榜单只取前 50；超出 50 名之后无法在页面查看名次。
- `stat_events` 无自动归档/TTL，长期将无限增长。

### 8.3 工程化 ⚠️
- **git 仓库已于 2026-09-21 建立本地基线**（`main` 分支，首次提交 `4edc3dc`，156 文件）；**远程推送仍未完成**（本机沙箱代理 `127.0.0.1:59788` 拒绝 CONNECT 到 github.com:443，返回 502；系统另有 `7897` 代理但沙箱未走）。**这台电脑仍是唯一一份代码，且已上线运营——远程备份是当前最高优先级事项，建议在沙箱外手动推送一次。**（仓库无任何密钥/凭证，可安全推公开或私有远程。）
- 无自动化 CI；已有本地回归与体检脚本：
  - `node .workbuddy/scripts/run-tests.js` 一键回归（**第一道关卡即 BOM 自检**，共 9 个测试文件）
  - `node .workbuddy/scripts/scan-project-health.js` 静态体检（绑定/dataset/路由/wx:key）
  - `node .workbuddy/scripts/check-bom.js` 编码自检（源文件禁止 UTF-8 BOM）
  - 改搜索匹配逻辑后**必须**跑 `test-searchline.js`（端到端 mock DB）+ `test-place-match.js`（规则单测）
  - 改公司聚合逻辑后跑 `test-searchcompany.js`（含元信息 R-18 的护栏断言）
  - **改详情页 WXML 后必跑 `scan-project-health.js`** —— v6.1 实测有效：删除网点拨号按钮后，`onCallGroup` 变成读 `dataset.index` 的孤儿绑定，体检立刻报 P0。
  - **任一层级地区数据改动后必须重跑** `node .workbuddy/scripts/sync-region-data.js`（见 R-16）
- **样式批量迁移脚本**（一次性，已完成，留档备查）：`.workbuddy/scripts/remove-glass.js` —— 全站移除毛玻璃（v6.2，见 R-23）。**幂等**，带 `--dry` 试运行与残留自检；改样式批处理时可直接复用其框架。
  - ⚠️ **该脚本的踩坑必须记住**：提纯白色**只能作用于底色类属性**（`background*` / `border*` / `box-shadow`），**`color` 一律跳过**。深色渐变头图上的白色文字/占位符被误提纯后，与白底放在一起会直接消失。
- **编码红线**：改本项目的 `.wxss` / `.wxml` / `.js` / `.json` 一律用编辑器/写入工具，**不得用 Windows PowerShell 的 `Out-File` / `Set-Content` / `>`**（默认写入 UTF-8 BOM，微信编译器会在第 1 行第 1 列报 `unexpected '\uFEFF'`）。

### 8.4 上线状态与待办（合规与配置）

> ✅ **2026-09-21 微信审核已通过，小程序正式上线。** 以下为上线时点的合规检查结论与上线后待办。

| 项 | 说明 |
|---|---|
| ~~服务类目~~ | ✅ **已完成**：用户已选「**工具 → 信息查询**」，个人主体可申请、无需资质，与"只做信息查询不做承运"的产品定位一致（避开了需企业资质的物流/交通类类目） |
| ~~协议主体信息~~ | ✅ **已完成**（v4.1）：两份协议已载明运营主体 **陈镇**（个人开发者）与客服电话，隐私政策含"个人信息处理者"（法定要求） |
| 隐私保护指引 | 🟡 **后台已填 3 项**（选中的文件、剪贴板、位置信息），但**剪贴板用途写错**：仍写「用于复制兑奖码、客服微信号」，而兑奖功能已下线。**须改为「用于复制客服微信号、专线地址、导出的专线数据」**（与小程序内《隐私政策》第 5 条保持一致）。另：openid / 使用统计两项若后台有对应可选项建议补声明，无则可略 |
| ~~隐私政策（小程序内）~~ | ✅ **已完成**（`pages/agreement/index.js`）：5 类处理行为逐条列明，无兑奖码残留，与代码实现一致 |
| 数据来源授权 | 🟡 已确认为**网上搜集整理**（2026-09-21）。展示的均为企业公开工商信息，风险可控但非零。**建议**在详情页/联系页加「企业可申请更正或下架」提示作为护身符；已扫出数据质量问题：电话格式错误（如 `031721785205` 12位）、济南诚惠物流 73 条线路挂 39 个异地手机号（疑似整批采集）。价格 `priceDesc` 填写率 42.7%，**建议维持现状不再补录**（展示不确定价格风险高于不展示） |
| ~~数据库索引~~ | ✅ **已完成**（2026-09-21）：13 条索引已在云开发控制台全部导入成功（lines 7 / stat_events 3 / favorites 3，含 favorites 唯一索引）。三份 JSON 定义见 `cloudfunctions/createIndexes/` |
| 客服二维码 | `utils/contact.js` 的 `qrcodeFileID` 为空，如需展示需上传云存储后填入 |
| ~~基础库版本~~ | ⚪ **不需要处理**：审核已通过并上线，`libVersion:"trial"` 保持现状，**上线后不得随意改动配置**（改动需重新提交审核）。后续迭代时再统一对齐稳定版 |
| **云函数待上传（v6.0/v6.1/v6.3/v6.4）** | ⬜ **阻断项**：① `cloudfunctions/searchCompany` —— v6.1 新增元信息、**v6.3 改了 `collectStations` 网点分组**，不上传则「查公司」tab / 公司详情页不可用且网点分组修复不生效；② `cloudfunctions/searchLine` —— 因 `regionData.js` 增加导出需一并重传；③ **`cloudfunctions/reportFeedback`（v6.4 新增）** —— 不上传则纠错表单提交提示"云函数未部署" |
| **重新提交审核（v6.0~v6.4）** | ⬜ v6.0 信息架构变更（tabBar 3 tab、新增 3 页、新增组件、新增云函数）+ v6.1 两个详情页改版 + v6.2 全站移除毛玻璃 + v6.3 网点分组修复 + v6.4 新增纠错表单页（页面数 17→18），**必须重新提交审核**后方可在正式环境生效 |
| **`feedback` 集合首启** | ⬜ 云函数 `reportFeedback` 首次调用会自动建集合（自愈）。但**建议在云开发控制台预先建好并加索引**：`_openid + day`（频控查询用，与 `stat_events` 同款）。不建也能跑，只是频控 count 会走全表扫描 |
| 图标资源 | 🟡 `assets/tabbar/` 6 个图标已全部就位（`home` / `route` / `company` 各一对，含 `-active` 态）。其中 `route` / `company` 两对为**脚本生成的示意级线稿**（`.workbuddy/scripts/gen-tabbar-icons.js`，81×81 8bit RGBA）。有专业设计稿时**直接替换 4 个 PNG 即可，无需改动 `app.json`** |

### 8.5 功能增强 ⬜
- 网点级坐标（当前为线路级单坐标，`KM` 距离显示位已预留但数据全为 0，未点亮）。
- 用户端"附近专线"（依赖网点级坐标）。
- ~~线路评价/纠错反馈入口~~ → ✅ **v6.0 落地入口，v6.4 落地表单**：专线详情页与公司详情页均有「信息有误？点这里报错」入口（`F-REPORT-01`），
  v6.4 起跳转到**站内结构化表单** `pages/report`（`F-REPORT-02`），反馈落 `feedback` 集合。**后台工单处理界面尚未做**，
  目前需在云开发控制台手工查看 `feedback` 集合并改 `status`。
- 收藏分组/备注。
- 公司可申请更正或下架的自助入口（当前仅详情页提示语，见 §8.4）。

---

## 9. 附录

### 9.1 批量导入字段速查
详见 `docs/批量导入说明.md`。要点：
- 必填 `title` / `fromCityName` / `toCityName`；建议填 `companyName`（**VIP 按公司批量开通依赖此字段精确匹配**）。
- 网点推荐用 `fromOutlets`/`toOutlets`，一行一个网点，格式 `地址 | 电话`，多个电话逗号分隔。
- 城市名**自然写法即可**（服务端自动规范化）；区县级建议写入 `toAreas`。
- 模板：`docs/专线导入模板-空白版.json`（推荐，干净无占位符）。

### 9.2 本地脚本清单（`.workbuddy/scripts/`）
| 脚本 | 用途 |
|---|---|
| `run-tests.js` | 一键回归全部测试（含 BOM 自检第一关） |
| `scan-project-health.js` | 静态体检（绑定/dataset/路由/wx:key）——**改 WXML 后必跑** |
| `check-bom.js` | 编码自检（源文件禁止 UTF-8 BOM） |
| `test-searchline.js` | searchLine 端到端（mock DB） |
| `test-searchcompany.js` | searchCompany 公司聚合 + 元信息（R-17/R-18）+ v6.3 网点归并 |
| `test-outlet-pairing.js` | **v6.3 网点分组**（单地址多电话=1 组，R-13 读端口径） |
| `test-reportfeedback.js` | **v6.4 反馈提交校验**（白名单/截断/图片归一/频控/openid/异常，61 断言） |
| `test-place-match.js` | 地名匹配规则单测 |
| `test-outlets-parse.js` | 网点解析单测 |
| `test-adminline-arrays.js` | adminLine 数组字段单测 |
| `test-adminline-heal.js` | adminLine 缺 id 自愈 |
| `test-adminstats-count.js` | adminStats 计数口径 |
| `test-region-coverage.js` | 行政区数据覆盖 |
| `sync-region-data.js` | 地区数据同步（云函数 → 前端副本，见 R-16） |
| `gen-tabbar-icons.js` | tabBar 图标生成（纯 Node 手写 PNG） |
| `check-requires.js` / `check-detail-layout.js` | 依赖引用 / 详情页布局自检 |
| `apply-design-tokens.js` | 设计令牌批量收敛 |
| `remove-glass.js` | v6.2 全站移除毛玻璃（幂等，带 `--dry` + 残留自检） |
| `fix-import-json.js` / `fix-shandong-json.js` | 导入数据修复模板 |

### 9.3 需求编号索引
| 编号 | 名称 | 端 |
|---|---|---|
| F-HOME-01 | 首页分流入口 | 用户 |
| F-HOME-02 | 首页公告 | 用户 |
| F-HOME-03 | 首页广告位 | 用户 |
| F-QUERY-01 | 按地址查专线 | 用户 |
| F-REGION-01 | 地区选择弹层（组件） | 用户 |
| F-COMPANY-01 | 查公司（搜索） | 用户 |
| F-COMPANY-02 | 公司详情（聚合视图） | 用户 |
| F-REPORT-01 | 信息纠错入口 | 用户 |
| F-REPORT-02 | 信息纠错表单 | 用户/服务端 |
| F-SEARCH-01 | 专线搜索（直达+中转） | 用户/服务端 |
| F-LIST-01 | 专线列表展示 | 用户 |
| F-LIST-02 | 列表筛选（直达/天天发车/时效） | 用户 |
| F-DETAIL-01 | 专线详情 | 用户 |
| F-FAV-01 | 收藏 | 用户 |
| F-USER-01 | 我的 | 用户 |
| F-CONTACT-01 | 联系客服 | 用户 |
| F-AGREE-01 | 协议与政策（三份） | 用户 |
| F-ADMIN-01 | 管理看板 | 管理 |
| F-ADMIN-02 | 专线列表管理 | 管理 |
| F-ADMIN-03 | 专线编辑 | 管理 |
| F-ADMIN-04 | VIP 批量按公司开通 | 管理 |
| F-ADMIN-05 | 广告管理 | 管理 |
| F-ADMIN-06 | 公告管理 | 管理 |
| F-ADMIN-07 | 月度统计 | 管理 |
