# PRD · 物流专线查询微信小程序

> 版本：v1.0（**基线副本**）
> 日期：2026-09-19
> 状态：需求已确认，已上线运营
> 依据：经十问十答确认的需求基线 + 功能结构与交互设计确认稿

---

> ## ⚠️ 关于本文件的版本（2026-09-22 补记）
>
> 本文件是**从旧工作区 `G:\workbuddy\wuliuzhaunxianchaxun` 找回的 v1.0 副本**。
>
> 历史记录中提到过一份 **v4.3** 的 PRD（含需求编号 `F-{模块}-{序号}` / `R-{序号}`、
> 数据一致性护栏 `R-10~R-15`、以及第 8.4 章「上线状态与待办」），
> 但那份 v4.3 **从未进入本仓库的版本库**（`git ls-files "*.md"` 查无此文件），
> 当前工作区也找不到 —— 现已丢失。
>
> **因此：本文件只作为需求基线参考，不能当作「最新实现」的准确描述。**
> 代码已演进到上线版本（VIP 到期模型、月度统计、埋点频控、双键制/自愈迁移等
> v4.x 内容均不在本文件里）。**两者冲突时以代码实现为准。**
>
> 本轮（2026-09-22）的两处需求变更已补记在下方「变更补记」章节。

---

## 一、产品目标和使用场景

### 1.1 一句话定位

一个帮货主和司机**"找专线、找公司"**的查询工具小程序。

以前找专线靠熟人介绍或在群里打听，信息零散且经常过时。本产品把"哪家公司跑哪条线"整理成可查询的数据库，用户打开就能查、查到就能打。

### 1.2 产品目标

| 阶段 | 目标 |
|---|---|
| 第一版 | 把查询链路做扎实并上线，验证"有没有人真的需要这个查询" |
| 后续 | 主体升级为个体户/企业后，补齐广告与兑奖，形成商业化闭环 |

**明确不做的事**：第一版不刻意做留存机制（不设积分、不做签到、不做订阅提醒）。理由：还没有真实用户数据，此时设计的留存手段都是猜的。等看到数据再决定。

### 1.3 目标用户与典型场景

| 用户 | 典型场景 | 他要解决什么 |
|---|---|---|
| 货主 / 发货方 | 我有货要从济南发到广州，找谁能拉 | 需要一份**跑这条线的公司名单**，能直接打电话 |
| 货主 / 发货方 | 之前合作的公司涨价了 / 跑了，想换一家 | 需要**同线路的替代公司**对比 |
| 司机 / 同行 | 听说"XX物流"挺大，它到底跑哪些线 | 需要**这家公司的线路覆盖全貌** |
| 新客户 | 我要发一条冷门线路，不知道有没有人跑 | 需要**确认这条线是否存在**，没有的话换中转方案 |
| 老用户 | 电话打过去是空号 | 需要**报错入口**，把错误信息反馈掉 |

### 1.4 运营方（用户本人）的核心诉求

- 数据自己可控：能录、能改、能批量导、能审核用户纠错。
- 主体限制下能上线：个人主体限制广告与兑奖，故第一版聚焦查询。
- 不返工：隐私合规、数据字段、免责声明等硬约束在第一版就做进去。

---

## 二、整体页面架构和导航结构

### 2.1 页面架构图

```
                       ┌──────────────────┐
                       │   ① 首页总览      │
                       │ 搜索框·热门·更新  │
                       └────────┬─────────┘
                                │
                 ┌──────────────┴──────────────┐
                 ▼                             ▼
        ┌─────────────────┐          ┌─────────────────┐
        │ ② 按地址查专线   │          │ ③ 按公司查线路   │
        │ 选出发地+目的地  │          │ 搜公司名         │
        └────────┬────────┘          └────────┬────────┘
                 │                             │
                 ▼                             ▼
        ┌─────────────────┐          ┌─────────────────┐
        │  线路详情页      │◄────────►│  公司详情页      │
        │  该线路全部公司  │          │  该公司全部线路  │
        └─────────────────┘          └─────────────────┘
                 │                             │
                 └──────────┬──────────────────┘
                            ▼
              ┌──────────────────────────┐
              │ ⑤ 纠错入口（各页常驻）   │
              └──────────┬───────────────┘
                         ▼
              ┌──────────────────────────┐
              │ ⑥ 网页版管理后台（电脑） │
              └──────────────────────────┘
```

### 2.2 两条主路径的定位差异

| | ② 按地址查专线 | ③ 按公司查线路 |
|---|---|---|
| 用户起点 | 只知道货和目的地 | 只知道公司名 |
| 数据方向 | 线路 → 公司集合 | 公司 → 线路集合 |
| 结果形态 | **横向对比**（多家公司并排比） | **纵向展开**（一家公司铺开看） |
| 核心页面 | 线路详情页 | 公司详情页 |

**关键设计原则：两个详情页方向相反，功能与字段排布必须差异化，不能做成同一个列表。**

- 线路详情页是「比价台」→ 字段垂直对齐，方便一眼扫完
- 公司详情页是「档案页」→ 分组展开，强调这家公司的覆盖广度

### 2.3 导航结构

- **底部 Tab**：仅 2 个 —— 「查专线」（承载 ①②）与「查公司」（承载 ③）。不做第三个 Tab，避免功能单薄时显得空。
- **首页即 ②③ 的入口页**，不作为独立 Tab。
- **详情页为二级页**，从列表点击进入，顶部导航返回。
- **纠错入口**以页面角落小按钮形式常驻，不占 Tab。
- **管理后台为独立网页**，不在小程序内，通过手机管理入口不作为第一版功能。

---

## 三、首页的内容结构

首页要回答一个问题："**你这小程序是干嘛的，我怎么用**"。用户要在 3 秒内明白。

从上到下依次为：

| 区块 | 内容 | 作用 |
|---|---|---|
| 1. 顶部搜索框 | 一个输入框，同时支持搜城市和搜公司名，系统自动判断类型 | 给"目标明确"的用户提供最快路径 |
| 2. 快捷入口 | 「我要发货」→ 直达 ② ；「我知道公司名」→ 直达 ③ | 给"不知道该怎么查"的用户两条明确的路 |
| 3. 热门专线 | 高频线路卡片，如 济南→广州、济南→上海、济南→临沂 | 给"没想法"的用户提供现成的查询对象，点击直达线路详情页 |
| 4. 最近更新 | 最近被修正/新增的公司条目，展示"XX物流 · 3天前更新" | 传递"数据是活的、有人在维护"的信任感 |
| 5. 底部公示 | 免责声明入口 + 数据更新时间说明 + 隐私政策入口 | 合规必需，不可省略 |

**首页不做的事**：不做轮播 Banner（第一版无广告素材）、不做登录引导弹窗、不做定位授权强制要求。

---

## 四、每个模块的用途

### 模块 01 · 首页总览

- **用途**：消除"这是干嘛的"的疑惑，提供三条明确的进入路径（搜索 / 快捷入口 / 热门线路）。
- **差异化特征**：唯一的"无输入也能用"的页面，靠预置内容和快捷入口驱动。

### 模块 02 · 按地址查专线

- **用途**：解决"我有货要从 A 发到 B，谁跑这条线"。
- **功能清单**：
  - 出发地 + 目的地两级选择器（省 → 市逐级选，也可直接输关键字）
  - 结果筛选：是否直达 / 是否天天发车 / 时效范围
  - 结果卡片：公司名、电话（一键拨打）、时效、备注
  - **一键反向查**：交换出发地与目的地，查回程线路（本模块独有）
  - 空结果引导：推荐相邻城市 / 提示可提交需求
- **差异化特征**：唯一有"反向查询"能力的模块，服务回程需求。

### 模块 03 · 按公司查线路

- **用途**：解决"我听说 XX 物流，它到底跑哪些线"。
- **功能清单**：
  - 公司名模糊搜索，支持简称与常见错别字容错
  - 公司详情页：全称、简称、总部地址、电话、经营主体、简介、是否已核实
  - 「覆盖线路」**默认平铺展示全部线路**（v5.3 改，见变更补记 C-02）
    - ~~「覆盖线路」按出发地城市分组展示所有目的地~~（已改为可选开关，不再默认）
  - **与我相关**：优先突出该公司在用户所在城市的线路（本模块独有）
  - 跳转线路详情：点击某条线路可跳到"跑这条线的所有公司"
- **差异化特征**：唯一以"单主体"为核心的模块，强调一家公司的覆盖广度。

### 模块 04 · 线路详情 / 线路公司列表

- **用途**：解决"就这一条线，把所有跑的公司摆出来让我挑"。
- **功能清单**：
  - 页面头部固定显示：线路名（如 济南 → 广州）+ 公司总数
  - 排序：综合 / 最近更新 / 公司规模 / 时效
  - 每行字段：公司名 + 电话 + 时效 + **数据更新时间**（如"3天前更新"）
  - 标签过滤：只看直达、只看天天发车
  - 字段横向对齐，保证可扫读
- **差异化特征**：全站信息密度最高的页面，唯一以"对比"为目的的页面。

### 模块 05 · 数据纠错

- **用途**：解决"我看到电话是错的，想告诉你"。这是数据质量的闭环保障。
- **功能清单**：
  - 从任意信息页触发，**自动携带当前记录的上下文**（不需要用户重新描述是哪条）
  - 纠错类型：电话有误 / 公司已停业 / 线路已取消 / 信息不完整 / 其他
  - 支持上传凭证截图（营业执照、聊天记录等）
  - 可选留联系方式（便于回访核实）
  - 提交后提示"已收到，我们会核实"，**不做即时生效**
  - **限频**：同一用户对同一条记录设提交次数上限，防恶意刷
- **差异化特征**：不产出信息，只回收信息，是唯一"用户 → 运营方"方向的功能。

### 模块 06 · 网页版管理后台

- **用途**：解决"我在电脑前，要录数据、改数据、审纠错"。
- **功能清单**：
  - Excel 批量导入，含**字段映射**与**导入前错误预览**
  - 公司表、线路表分页浏览 + 搜索 + 单条增删改
  - 纠错审核队列：逐条查看，采纳 / 驳回 / 标记待定
  - 数据质量看板：缺电话条数、超 90 天未更新条数
  - 登录：管理员账号密码，不做注册入口
- **差异化特征**：唯一的 Web 端、唯一的写操作入口、唯一需要登录的功能。

### 贯穿性要求（非模块，但必须做）

1. **隐私合规**：首次进入弹隐私协议；提供《用户信息处理规则》页面；每个接口在人能看懂的地方写明用途。个人主体查询类小程序的硬门槛，不做直接驳回。
2. **数据更新时间**：每条数据必须带 `updatedAt`，且在页面上展示。用户需要知道信息的新旧。
3. **免责声明**：「信息由公开渠道整理，仅供参考，请自行核实」。可挡掉大量纠纷。

---

## 五、第一版必须完成的功能（P0）

| # | 内容 | 说明 |
|---|---|---|
| 1 | 云开发环境搭建 | 微信云开发（CloudBase）作为后端 |
| 2 | 建立 6 张数据表 | companies / routes / route_companies / cities / corrections / admins |
| 3 | 导入首批数据 | 手工整理的 20~50 家公司，用于验证产品形态 |
| 4 | 首页总览 | 含搜索框、快捷入口、热门专线、最近更新、底部公示 |
| 5 | 按地址查专线 | 含两级选择器、筛选、结果卡片、一键反向查、空结果引导 |
| 6 | 按公司查线路 + 公司详情页 | 含模糊搜索、覆盖线路分组、与我相关 |
| 7 | 线路详情 / 线路公司列表 | 含头部固定信息、多维排序、更新时间展示、标签过滤 |
| 8 | 数据纠错入口与提交 | 含上下文携带、类型分类、凭证上传、限频防恶意 |
| 9 | 网页版管理后台 | 含 Excel 导入、双表增删改、纠错审核队列、质量看板 |
| 10 | 隐私合规三件套 | 隐私协议弹窗 + 用户信息处理规则页 + 接口用途说明 |
| 11 | 免责声明与更新时间标注 | 在首页与各详情页展示 |

**贯穿三条硬约束**：隐私合规、数据更新时间、免责声明 —— 属于 P0，不可延后。

---

## 六、第一版暂时不做的功能（P1 / P2）

| 功能 | 为什么不做 | 后续条件 |
|---|---|---|
| **官方流量主广告** | 个人主体无法开通，做了也上不了线 | 升级为个体户/企业主体后 |
| **广告位实装** | 无广告主，做了也是空位 | 有广告主后启用；**但数据库留字段、页面留位置** |
| **抽奖 / 兑奖功能** | 个人主体基本不批，且微信对抽奖类审核极严 | 主体升级后，且避开诱导分享设计 |
| **公司认领自己的页面** | 第一版没有公司会来认领，做注册+资质审核体系纯属浪费 | 有流量、有公司主动联系后 |
| **全国范围数据** | 数据质量比覆盖面重要，先做山东/华东 | 区域内数据稳定、纠错机制跑通后 |
| **收藏 / 订阅 / 消息通知** | 不刻意做留存，先看真实数据 | 有数据支撑留存决策后再评估 |
| **微信支付** | 个人主体开不了，且当前无收款场景 | 主体升级 + 有明确收款需求 |
| **小程序内管理员入口** | 手机上录数据、导 Excel 都不现实 | 视运营需要再评估 |
| **采集脚本的自动化调度** | 第一版数据由手工整理，尚不需要 | 数据扩张阶段（阶段三） |

### 明确禁止的设计（合规红线）

- **禁止"分享给好友获得抽奖/奖励机会"类设计** —— 微信明令禁止诱导分享，是最常见的驳回原因。
- **禁止在无资质情况下上线任何涉及随机利益的功能**。
- **禁止照搬他人整站数据库** —— 公开信息可以采集，但必须清洗重组，成体系地照抄有版权风险。

---

## 七、可以实际检查的验收标准

> 以下每条都可实际操作验证，不是主观描述。

### 7.1 阶段一 · 骨架跑通（结束后可提审上线）

| # | 验收项 | 检查方法 | 通过标准 |
|---|---|---|---|
| A1 | 云环境可用 | 小程序能成功请求云数据库并返回数据 | 列表页有真实数据渲染，无报错 |
| A2 | 数据表建成 | 在云开发控制台查看集合 | 6 张表均存在且字段与设计一致 |
| A3 | 冷启动完整链路 | 全新用户打开小程序，不登录也能完成一次查询 | 首页 → 选城市 → 线路详情 → 看到公司 → 点击电话可拨号，无死链 |
| A4 | 按公司查链路 | 输入某公司简称 | 能搜到该公司，进入详情页能看到其全部线路 |
| A5 | 反向查询可用 | 在结果页点"反向查" | 出发地与目的地互换，返回回程线路结果（无数据时返回空状态而非报错） |
| A6 | 空结果不崩 | 查询一条不存在的线路（如 拉萨→某小城） | 显示空状态引导文案，无白屏、无控制台报错 |
| A7 | 匿名可用 | 清除小程序缓存后重进 | 不登录、不授权仍可完成全部查询与拨号 |
| A8 | 更新时间可见 | 查看任意线路详情行 | 每行能看到"X天前更新"字样 |
| A9 | 免责声明可查 | 首页底部 | 有免责声明入口，点开有完整内容 |
| A10 | 隐私合规齐全 | 首次进入小程序 | 弹出隐私协议；能找到《用户信息处理规则》页 |
| A11 | 拨号可用 | 点击任意电话号码 | 唤起系统拨号盘且号码正确 |
| A12 | 提审材料 | 提交微信审核 | 具备隐私协议、类目资质说明（个人主体查询类） |

### 7.2 阶段二 · 后台与纠错闭环

| # | 验收项 | 检查方法 | 通过标准 |
|---|---|---|---|
| B1 | 后台可登录 | 用管理员账号登录网页后台 | 登录成功，未登录访问被拦截 |
| B2 | Excel 导入可用 | 上传一份含 10 条记录的测试 Excel | 显示字段映射界面与错误预览，确认后入库，小程序端能查到 |
| B3 | 导入容错 | 上传一份含错误数据（缺电话、格式错）的 Excel | 明确标出错误行与原因，拒绝或跳过，不污染数据库 |
| B4 | 单条编辑可用 | 在后台修改某公司电话 | 保存后小程序端刷新可看到新号码 |
| B5 | 纠错提交可用 | 在小程序任意详情页提交一条纠错 | 后台审核队列出现该条，且带正确的目标记录上下文 |
| B6 | 纠错审核可用 | 在后台采纳一条纠错并修改数据 | 状态变更，修改写入正式数据 |
| B7 | 纠错限频生效 | 同一账号对同一条记录连续提交多次 | 超过阈值被拦截并提示，不产生垃圾数据 |
| B8 | 质量看板可用 | 打开后台数据质量看板 | 缺电话条数、超 90 天未更新条数与实际情况一致 |

### 7.3 阶段三 · 数据扩张与打磨

| # | 验收项 | 检查方法 | 通过标准 |
|---|---|---|---|
| C1 | 规模化导入 | 导入 1000 条以上线路数据 | 无超时、无丢失，条数与源数据一致 |
| C2 | 查询性能 | 数据量达千条级后执行最热线路查询 | 列表首屏返回时间在可接受范围内，无明显卡顿 |
| C3 | 搜索性能 | 搜索公司名 | 结果返回及时，模糊匹配生效 |
| C4 | 分页正确 | 线路公司列表数据超过一页 | 上拉加载正常，无重复项、无漏项 |
| C5 | 采集清洗可控 | 运行采集脚本 | 产出可预览的表格，人工确认后才入库 |

---

## 附：数据表结构（开发依据）

### companies · 公司表
```
_id, name 全称, shortName 简称, initial 首字母,
phone 主电话, backupPhone 备用电话,
address 地址, city 所在城市, province 所在省,
scale 规模, intro 简介,
verified 是否已核实, updatedAt 更新时间
```

### routes · 线路表（枢纽）
```
_id, fromCity, fromProvince, toCity, toProvince,
fromCityId, toCityId,
routeKey 用于去重, companyCount 冗余计数, updatedAt
```

### route_companies · 关联表（多对多，承载线路属性）
```
_id, routeId, companyId,
transitDays 时效, isDirect 是否直达,
frequency 发车频率, priceNote 价格备注, remark 备注, updatedAt
```

> **关键设计取舍**：「时效」「是否直达」等属性必须放在本表。同一家公司跑济南→广州 2 天、跑济南→乌鲁木齐 5 天，塞进 companies 或 routes 任一张表都是错的。

### cities · 城市字典
```
_id, name 城市名, province 所属省,
pinyin 拼音, initial 首字母, isHot 是否热门, sortOrder 排序
```

### corrections · 纠错表
```
_id, targetType 公司/线路, targetId 记录ID,
type 错误类型, content 说明, images 凭证图,
contact 联系方式, openid 提交人, status 待审/采纳/驳回
```

### admins · 管理员
```
_id, username, passwordHash, role 角色, lastLoginAt
```

---

## 附：三阶段开发计划

| 阶段 | 内容 | 可上线状态 |
|---|---|---|
| 阶段一 · 骨架跑通 | 云环境 + 6 张表 + 首批数据 + 页面 1/2/3/4 + 隐私合规 | **结束后可提审** |
| 阶段二 · 后台与纠错闭环 | 网页后台 + Excel 导入 + 纠错提交与审核 + 质量看板 | 可并行开发 |
| 阶段三 · 数据扩张与打磨 | 采集脚本 + 批量导入 + 排序筛选优化 + 性能缓存 + 广告位占位 | 数据驱动，按需推进 |

---

## 风险提示（运营方需知悉）

1. **数据准确性责任在主体**：电话错误导致司机跑空，投诉到微信由主体承担。故纠错入口与免责声明不可省略。
2. **采集数据的版权**：公开信息可用，但需清洗重组，不可成体系照搬他人数据库。
3. **采集必须在电脑端进行**：小程序端不得内置爬虫，只能读取自己的云数据库。
4. **主体限制是当前最大约束**：广告与兑奖功能受个人主体限制无法上线，这是选择"路线一（先只做查询）"的根本原因。

---

## 变更补记（2026-09-22 · 上线后修复）

> 小程序已上线运营。以下为上线后依据真实用户反馈做的两处需求变更，
> 均已实现并回归通过。编号沿用 C-xx，避免与既有 A/B/C 验收项混淆。

### C-01 · 各模块必须具备「符合自身场景的基础功能」

**背景**：用户明确提出「每个模块都要符合自身场景的基础功能」。
上线版本多个页面只做了正常路径，异常与边界缺失。

**需求**：按页面场景划分，补齐对应能力，缺一不可。

| 场景 | 必需能力 |
|---|---|
| 列表 / 检索页 | 下拉刷新、触底加载、加载态、空态、**失败态（可重试）** |
| 表单页 | 提交防重、必填校验、成功反馈 |
| 静态文本页 | 无额外要求 |

**关键区分：空态 ≠ 失败态**
「没有这条线路」是**空态**（重试无用，应引导换条件或反馈）；
「网络/服务异常」是**失败态**（必须给「重新加载」按钮）。
混为一谈会让用户把故障误认为"没数据"，是本次重点修正项。

**落地**：8 个页面全部补齐，并新增自检脚本
`.workbuddy/scripts/check-module-basics.js`（改页面后必跑）。

### C-02 · 公司详情：同一公司的所有线路合并展示

**背景**：用户反馈「同一公司所有线路合并一起」。
实测 25 家多线路公司中 16 家（64%）横跨多个出发城市，
而改前按 `route.fromCity` **自动分组**，把这些线路拆进多个「从X出发」组
（如：中铁快运 4 条线路被拆成 济南 / 成都 / 乌鲁木齐 3 组），
与用户诉求正好相反。

**需求**：
- 公司详情页「覆盖线路」**默认平铺全部线路**，按「出发城市 → 目的地」排序，
  保证同城线路连续、不被拆散。
- 按出发地分组**降级为可选开关**（用户手动切换），不再作为默认视图。
- 切换视图不得丢行、不得重复。

**回归**：新增 `test/suites/company-detail-merge.test.js`，
用真实数据断言「平铺不丢不重、同城连续、切视图不丢数据」。

### C-03 · 列表分页

查专线页与查公司页改为分页加载（触底追加），避免一次渲染过长的列表。
注意：查专线页的筛选是**客户端**过滤，故触底时会循环补页，
直到攒够可展示条数或后端确实无下一页 —— 否则「当前页全被筛掉」
会让用户误判为没有直达/天天发车的公司。

---

## 附：工程自检脚本（2026-09-22）

| 脚本 | 用途 | 何时必跑 |
|---|---|---|
| `test/run-all.js` | 全量单元测试（6 套件） | 每次改逻辑 |
| `scripts/check-project.js` | 静态体检（编码/语法/绑定/铁律） | 每次改前端 |
| `scripts/check-acceptance.js` | PRD 验收自检 A1~A12 | 提审前 |
| `scripts/check-deploy.js` | 云函数部署前检查 | 传云函数前 |
| `scripts/smoke-admin.js` | 管理后台冒烟 | 改后台后 |
| `.workbuddy/scripts/check-module-basics.js` | 各页「场景基础能力」自检 | **改页面后** |
| `.workbuddy/scripts/check-bom.js` | BOM 自检 | **提交审核前** |

---

*本文档为后续开发的唯一需求依据。任何超出本文档范围的功能，需先修订本文档再行开发。*
*（注：v4.3 版本已丢失，本文件为 v1.0 基线副本 + 上线后变更补记；与代码冲突时以代码为准。）*
