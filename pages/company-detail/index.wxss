/* pages/company-detail/index.wxss
 *
 * 视觉语言全部来自 app.wxss 的 pf-* 类（与专线详情共用同一套），
 * 本文件只放"本页独有的微调"，避免与全局重复定义造成漂移。
 *
 * 参考图：齐鲁快运 公司详情 —— 白底描边头卡打头，无渐变头图。
 */

.pf-page { min-height: 100vh; background: transparent; }

/* 头卡与首张站点卡之间略收紧（头卡本身就是第一屏视觉重心） */
.pf-head-card { margin-top: 4rpx; }

/* 组头与首行的间距：参考图里组头是"区块标题"的观感，不要贴太近 */
.pf-group-head { margin-top: 2rpx; }

/* 无站点数据时不留空白：两端卡都是 wx:if，无值直接不渲染，此处无需处理 */
