# 2026-09-12

## Bug 修复：搜索"济南-万盛"无结果（万盛属重庆）
- 根因：searchLine/regionData.js 重庆下属区县表漏了"万盛"（万盛经开区），数据库第一级粗过滤 queryTokens('万盛') 不含"重庆"，线路根本捞不出来
- 修复：regionData 重庆 children 加 '万盛'；placeMatch 别名组加 ['万盛','万盛经开']（万盛经开区写法互通）
- 连带修正：placeKey 规范名规则从"组内取最长"改为"取组内第一个"——否则'万盛'会被归一成'万盛经开'断掉归属链（陵城/陵组不受影响，第一个即最长）
- 验证：9 项专项断言 + searchLine 原 9 项回归全过
- 部署提醒：searchLine 云函数有改动，需重新上传

## 区划表全量补漏（用户要求排查同类问题并统一修复）
排查出四类缺口，全部补齐（searchLine/regionData.js + placeMatch.js）：
1. 直辖镇：东莞 33 镇街（虎门/长安/常平/松山湖等）、中山 22 镇街（小榄/古镇等）从空数组补全
2. 海南省直辖县 10 个（澄迈/陵水/临高/定安/屯昌/昌江/乐东/陵水/保亭/琼中/白沙）
3. 新疆兵团市 7 个（铁门关/双河/昆玉/胡杨河/可克达拉/新星/白杨）
4. 国家级新区/合作区：雄安→保定、两江→重庆、天府→成都、西咸→西安+咸阳、贵安→贵阳+安顺、深汕→深圳+汕尾、西海岸/胶南→青岛、洋浦→儋州、滨海→天津
5. placeMatch 别名组补 ["雄安","雄安新"]等 4 组，解决"XX新区"输入经 norm 变"XX新"的断链
6. queryTokens/regionMatch/isParentCity 升级：所有别名变体都参与上级归属判定
- 新增 test-region-coverage.js（49 项断言）+ searchLine 原 9 项回归，全部通过
- 部署提醒：searchLine 需重新上传

## 用户要求"以小程序开发工程师视角检查代码逻辑修复 bug"
系统性排查修复 4 处：
1. **【中转搜索 bug】searchLine/index.js**: searchTransfer 用 `firstLine.id` 而非 `_id||id`；生产记录若缺 id 字段，`undefined===undefined` 会提前 return 让全部中转组合被跳过，wx:key 也会变成 `undefined-undefined` 全部冲突
   - 修复：抽 `kid(l) = _id||id || 'fromCityName|toCityName'` 合成键；同线路两端跳过；去重统一用 kid
   - 加 2 项专项回归（缺 id 数据生成中转 + key 非 undefined-undefined）
2. **【UX】admin/line-edit loadLine** 无 catch 兜底：失败或记录不存在时页面停留空白无提示
   - 修复：加 .catch + 找不到记录 toast
3. **【数据一致性】adminNotice**: `limit(1).get()` 无序匹配历史脏 doc；多 doc 时可能更新错对象
   - 修复：改为固定 _id 'current' 做幂等 upsert；同步前端 pages/index 与 pages/admin/notice 也按固定 _id 读
4. **【UX】admin/ad-edit loadAd** 缺 catch 兜底：网络异常无提示
   - 修复：加 .catch toast
- 60 项测试全过（searchLine 11、adminLine-arrays 10、region-coverage 49），全 JS 文件 `node --check` 通过
- 部署提醒：searchLine、adminNotice 需重新上传云函数；前端文件需重新编译
