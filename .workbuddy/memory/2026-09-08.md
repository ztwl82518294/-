# 工作日志 2026-09-08

- 完成小程序 v3.1 全量代码评审（app/页面/云函数/utils），产出《专业评估报告》→ `.workbuddy/reports/专业评估报告-2026-09-08.html`。
- 关键发现：getFavorites 无分页（>20 条收藏静默丢失）；adminLine list 云函数端 100 条上限；stat_events 前端直写可刷量；pages/demand、pages/admin/demands 死代码残留；搜索为客户端全量拉取+过滤需迁云函数；VIP 模型缺 vipExpireAt/续费。
- 整改批次：P0（1~1.5 天，审核前）→ P1（3~5 天）→ P2（1~2 天）。

## P0 批次已落地（19:55）
- `utils/storage.js`：getFavorites 改循环分页（PAGE=20, MAX=1000），修复收藏 >20 条丢失。
- `cloudfunctions/adminLine`：list 加 limit(1000)，修复管理列表 100 条截断。
- 新增 `cloudfunctions/statLog`（package.json/config.json/index.js）：埋点改服务端写入，_openid 显式注入，同用户单日 DAILY_CAP=30 条频控；`utils/stats.js` 改为调 statLog，不再直连写 stat_events。**需在开发者工具重新部署 statLog 与 adminLine 两个云函数**。
- 删除空死目录 `pages/demand`、`pages/admin/demands`（全项目无引用）。
- 所有改动文件 node 语法检查通过。
- 待用户手动完成（P0-5）：mp 后台 chooseLocation 接口权限 + 隐私指引声明 + 真机验证管理端地图选点。
- 下一步：P1 批次（搜索迁云函数、VIP 模型 vipExpireAt/续费、管理员配置收敛、save 白名单、错误上报、建索引）。

## P1 批次已落地（20:20）
- 新增 `cloudfunctions/searchLine`（复制 placeMatch/regionData）：搜索服务端聚合，单次 limit(1000)，返回前把 isVip 修正为"有效会员"口径；`pages/line-list` 重写为单次 callFunction。
- VIP 模型：新增 `utils/vip.js`（effectiveVip/applyEffectiveVip/fmtDate/vipStatusText）；lines 增加 vipExpireAt，adminLine list/batchSave 自愈迁移旧数据（isVip=1 无到期 → +1 年）；line-edit VIP 改为到期时间管理（+1个月/+1年/停用，isVip 由到期派生）；adminLine 新增 batchVip 按公司批量开通；admin lines 列表显示到期文本；storage/line-detail 读路径统一有效口径。
- 管理员白名单：adminLine/adminStats/prize 改读 `app_config` 集合 doc 'admins'（5 分钟实例缓存，FALLBACK 自愈写库）；config/admin.js 降级为前端展示用途。
- adminLine save 加字段白名单 + NUMBER 归一；statLog 顺带自愈创建 `app_errors`；app.js 加 onError/onUnhandledRejection 上报；createIndexes 目录补 README + 4 份索引 JSON（wx-server-sdk 不支持代码建索引，需控制台手动建，清单见 README）。
- 全部 19 个文件 `node --check` 通过。
- **待用户部署**：searchLine/adminLine/adminStats/prize/statLog 五个云函数需上传；控制台按 createIndexes/README.md 建索引；app_errors 集合由 statLog 自动创建。
- 剩余 P2：tabBar 图标、日志清理、管理后台分包、兑奖规则公示。

## P2 批次已落地（20:45）
- tabBar 图标：PIL 绘制三组 81×81 透明底线性图标（灰/蓝两态，home/star/user）→ `assets/tabbar/`；生成脚本存 `.workbuddy/scripts/gen_tabbar_icons.py`；app.json 已挂 iconPath。
- 管理后台分包：app.json 主包剩 9 页，pages/admin 下 8 页拆入 subPackage（root: pages/admin），普通用户不再下载管理后台代码。
- 日志清理：移除 openid/收藏数据等 console.log（storage/user/favorite/city-select/admin index）；查询历史时间格式固定 MM-DD HH:mm。
- 兑奖规则公示：prize 页两个分支底部均加 4 条活动规则（统计口径/奖励/核销/防刷声明）。
- 全项目 JS node --check 通过，app.json 校验通过（页面文件+图标齐全）。
- 评估报告全部批次（P0/P1/P2）已完成，剩余为用户手动部署项（5 个云函数上传、控制台建索引、chooseLocation 真机验证）。

## 全量代码复查（20:58，修复 7 处）
- adminAd / adminNotice 白名单硬编码残留（P1-3 遗漏）→ 收敛到 app_config/admins。
- city-select 城市列表、admin/ad-edit 线路下拉：客户端 get() 默认 20 条截断 → limit(1000)。
- transferList wx:key="index" 无效 → searchLine 生成组合键 key，wxml 改 wx:key="key"。
- adminStats totals.users 受榜单 limit(50) 影响口径失真 → 独立聚合真实活跃用户数（上限 1000）。
- 删除前端无引用死代码：utils/placeMatch.js、utils/regionData.js（searchLine 有独立副本）、utils/util.js（模板残留）。
- 全项目 node --check + app.json 完整性校验通过。
- 复查确认无问题：prize 状态机、agreement 协议内容、contact 页、getOpenid、admin 各页绑定、line-list wxml 与 searchLine 返回字段匹配。

## 平台规则合规检测（第三轮）
- 对照《微信小程序平台运营规范》《常见拒绝情形》逐项自查，修复 4 处：
  1. 首页空白广告位"招租"文案（拒绝情形 3.2.2）→ 只渲染已售广告位，空位整块隐藏
  2. 隐私政策未声明行为统计收集（规范 5.12）→ agreement 补第 8 条（搜索词+浏览+openid，仅用于活跃榜）
  3. app.json 缺 permission.scope.userLocation.desc（chooseLocation 授权必需）→ 已补
  4. 兑奖规则缺活动期限披露（规范 3.13）→ 补第 5 条
- 确认无违规：分享（无利益诱导）、虚拟支付（VIP 人工开通无支付按钮）、无 UGC、有联系方式页
- 遗留手动项：mp 后台隐私保护指引需同步声明位置/剪贴板/统计数据；类目需为物流服务（企业主体）；小程序 ICP 备案；奖品价值合规（<5 万）
