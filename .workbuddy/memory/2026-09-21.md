# 2026-09-21

> ⚠️ **本文已按主题精简。** 原文（688 行）归档在 `archive/2026-09-21.full.md`。
> 其中 **v4~v6.4 的全部内容（PRD v4.0 / Notion 风格视觉重构 v5.0~v5.2 / 广告模块 / 专线详情 v2 /
> 全站重设计 v6.0 / BOM 事故 / 月度兑奖删除 / 各类旧 UI 微调）对应的代码已随新 PRD 重写而全部删除**，
> 只保留在归档文件里供追溯，**不要照它开发**。
> 长期有效的规范已抽入 `MEMORY.md`。

## 当天两件事

### 1. 旧版（v4~v6.4）收尾 —— 代码现已全部删除
- 小程序曾上线并通过审核；期间完成 Notion 浅色视觉重构（v5.0 令牌 / v5.1 磨砂 / v5.2 渐变加深）、
  广告模块、专线详情 v2、全站重设计 v6.0（8 张参考图）、月度兑奖移除、协议补主体信息、
  类目确认、数据库索引建齐。
- **教训（仍有效）**：12 个 `.wxss` 被 PowerShell `Out-File` 写入 UTF-8 BOM 导致编译失败
  → 已升级为**项目级红线**，见 `MEMORY.md` 第五节。
- **教训（仍有效）**：`companyCount` 这类冗余计数宁可靠重算，不要 `+=1/-=1`。

### 2. ★ 按新 PRD 从零重写（本日主线）
> 用户原话：「按照这个重新设计，之前的全部删除，一点不留。」
> 四项确认：在 logistics 里重做 / 先提交再删 / 只放 20-50 条完整样板 / 真从零重写。

- **新形态**：6 张表（时效/直达/频率挂 `route_companies`）+ 小程序 8 页 + 网页后台。
  详见 `MEMORY.md` 第一、二节。
- **网页后台**：零依赖（只用 Node 内置 http/fs/crypto/zlib），三层分层 + 写穿透。

#### ★★ 当天最重要的事故：覆盖写清空数据
- `repository.flush()` 是**全量覆盖写**。未 `loadAll()` 就写 → 内存空表 → **整个数据文件被抹成 `[]`，不报错**。
- 诊断过程中真实触发一次，`admin/data` 四张表全被清空；因 `data/seed-data.js` 是唯一数据源，已重建（40/55/72）。
- **修复**：`repository` 加 `loaded` 标志，未装载调 `flush()` 直接抛错；`server.js` 的 `loadAll()` 不吞错。
- 详见 `MEMORY.md` 第三节。**这是本项目最严重的坑，别动那道护栏。**

#### 当天修掉的测试侧坑（都是断言写错，不是功能错）
- `/api/import/template` 三条断言失败 → 真因是**裸 `fetch` 不带 Cookie**，
  且 `req()` 会被 `/logout` 的**清空型 Cookie** 污染成字面量 `"lq_admin="`。
  修复：模板路由提到鉴权之前 + `req()` 只在非空 token 时覆盖 + 退出节加回归护栏。
- 验收脚本三条失败 → 真因是**猜错了实现位置**：云环境 ID 在 `app.js` 的 `CLOUD_ENV`（无 `config/` 目录）、
  反向查是页面级实现（不在 utils）、客服信息在 `utils/privacy.js` 的 `CONTACT`（无 `contact.js`）。
  修正断言，并删掉空的 `config/` 残留目录。

#### 当天新建
- `README.md`（后台启动、`admin/data` 定位、覆盖写护栏、BOM 红线、跨端一致性表）
- `docs/验收自检报告.md`（A1~A12 / B1~B8 / C1~C5 逐条结论 + 12 项人工清单 + 部署前检查清单）
- `scripts/check-acceptance.js`（验收自检）
- `export-seed.js` 增 `--admin-only`；`.gitignore` 增 `admin/data/` 与 `.tmp-smoke-backup/`
- 技能 `zero-dep-node-admin`

#### 当天验收结论
- **A1~A12** 59 静态断言全绿（12 项须人工）；**B1~B8 全部覆盖**；**C1~C5 未启动**
- 四层测试：335 + 9 类 + 59 + 72，全绿
