# 2026-09-10 工作日志

## Bug 修复：编辑页 (l.tags || []).join is not a function
- 根因：批量导入模板中 tags/toAreas 写成逗号字符串，写入库后为 string；
  line-edit 的 loadLine 按数组处理（.join(',')）→ TypeError 崩溃
- 三层修复：
  1. 写入端：adminLine 新增 ARRAY_FIELDS=['toAreas','tags'] + toArr()，
     pickFields 内统一归一为字符串数组（save/batchSave 全走此路径）
  2. 读取端兜底：line-edit 新增 arrToStr()（数组/字符串双向兼容）；
     line-detail 新增 toArr() 归一 tags/toAreas（否则 wx:for 遍历字符串逐字渲染）；
     searchLine 新增 normalizeLine()，返回前归一（line-list 依赖）
  3. 存量自愈：adminLine 新增 migrateArrayFields()，按 app_config/migrated_arrays_v1
     记录 cursor 分批扫描（list 第 1 页每次最多 1000 条），翻完一遍打 done 标记；
     另提供手动 action 'migrateArrays'
- 测试：新增 .workbuddy/scripts/test-adminline-arrays.js（10 项断言全过）；
  searchLine 原有 9 项回归测试仍通过
- 导入模板保持"逗号字符串"写法（更易手写），后端兼容两种格式
- 待部署：adminLine、searchLine 需重新上传

## 详情页 VIP 特效归属修正（用户截图反馈"特效要在专线名称上不是线路上"）
- 排查：vip-name-shine 全局只挂 title（详情 hero-title / 列表 line-title / 收藏），线路元素均无特效类，结构正确
- 真正问题：详情页 ::before 金色光晕 120%x160% + text-shadow 扩散 26~52rpx，向上溢出包住线路行"济南市 ⇀ 阜阳市"，观感上像线路在发光
- 修复（line-detail wxss）：halo 收紧为 100%x115%、透明度 .28→.16、呼吸 scale 1.15→1.08；text-shadow 6/14/26→4/10/16（呼吸峰值 52→30）；vipFlash drop-shadow 同步减小；title margin-top 12→18rpx 增加与线路行间距
- 效果：金色特效贴在"济南-阜阳专线"名称文字上，线路行完全无光晕沾染

## VIP 特效归属最终确认（用户红框标注：特效要挂在公司名上）
- 用户语义澄清：用户口中的"专线名称"= 公司名（济南波涛物流），"线路"= 标题（济南-阜阳专线）
- 调整：vip-name-shine（金色流光+闪电+光晕全套）从 hero-title 移到 hero-company（详情页）；
  列表/收藏页 line-title 去掉 vip-name-shine，公司名保留 vip-company-shine
- app.wxss vip-company-shine 增强：渐变提亮（#92400e→#b45309，加 #fffbeb 高光带），扫过时白金亮带明显
- 详情页公司名字号 26→30rpx 加粗；hero-title 回归纯白粗体
- 教训：用户对页面元素的称呼与开发者术语不同（公司名被叫"专线名称"），UI 反馈务必让用户红框标注定位

## 详情页红框区排版对调
- hero 区顺序改为：线路行 → 公司名（42rpx 加粗白色，VIP 挂金色特效）→ 专线名称（30rpx 白色）→ 徽章
- 即公司名与专线名称上下位置互换、字号互换（用户明确要求）

## 广告编辑页"跳转专线"改为可搜索选择器
- 问题：原生 picker 滚轮在万股级线路下无法找到目标公司，且旧实现 limit(1000) 仍截断
- 改造：点击"跳转专线"弹底部搜索面板，300ms 防抖服务端正则检索（标题/公司名/出发/目的城市，最多 20 条），点选绑定；已选显示标题带 ✕ 清除
- 编辑回显：按 ad.lineId 精确查标题（不再依赖全量 lineNames）
- 防过期回填：结果仅在弹窗未关且关键词未变时 setData

## 详情页"地址与联系"按站分组 + 电话分列（用户截图要求：发站地址+发站电话 / 到站地址+到站电话）
- 数据模型：lines 新增 fromPhone/toPhone（adminLine 白名单 + 编辑页两个输入框 + 导入模板/文档）；phone 保留为兼容字段（= fromPhone），老数据两端回退 phone
- 详情页：地址与联系卡重排为两组——🚚发站（📍发站地址+📞发站电话）/ 🏁到站（📍到站地址+📞到站电话），组头左侧色条（发站蓝/到站绿）；拨打按钮按站取号（data-station），底部"立即拨打"用合并列表
- 编辑页：电话必填改为发站/到站至少填一个；老数据加载时 fromPhone/toPhone 用 phone 回填，保存后写回新字段
- 部署提醒：adminLine 有改动需重新上传

## 导入模板电话字段改造（发站/到站区分）
- 模板去掉 phone 必填位：fromPhone【填】/ toPhone【填】成为主字段，两份模板 + 说明文档同步更新
- adminLine pickFields 兼容派生：phone 缺失时自动 = fromPhone || toPhone（老版本详情页/分享卡片仍读 phone）
- 回归：adminline arrays 10 项测试通过，模板 JSON 校验通过
