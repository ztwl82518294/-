# 2026-09-16

## 修复：详情页"地址与联系"显示"未填写"（用户截图报告）

**根因**：山东诚惠 35 条专线数据经旧版 adminLine 云函数批量导入（或控制台直灌），`fromOutlets`/`toOutlets` 存成字符串 `"地址 | 电话1,电话2"` 且 `fromAddress`/`fromPhone` 等老字段全空；详情页 `buildOutlets()` 只认数组 → 两头落空显示"未填写"。

**修复（4 处代码 + 1 个数据文件）**：
- 新建 `utils/outlets.js`：`parseOutlets/parseRow/deriveLegacy`，读端把字符串网点归一为 `[{addr,phone}]` 并反填空老字段（口径同服务端 deriveOutlets）
- `pages/line-detail/index.js`：loadLine 中对 from/to 调 `deriveLegacy` → 发站/到站展示、复制地址、立即拨打全部恢复
- `pages/admin/line-edit/index.js`：加载时 deriveLegacy 反填表单，管理员能看到真实数据；修复"打开即保存会用空值冲掉库里网点"的隐性数据丢失
- `cloudfunctions/adminLine/index.js`：deriveOutlets 加两道护栏（①网点/地址/电话三者都未提交时不动网点字段；②单地址多电话重建时保持 1 个网点挂多号，不再按下标拆散）；toOutlets 空值判定收紧为 `!v`
- `.workbuddy/scripts/fix-shandong-json.js` → 生成 `E:\微信小程序公司信息\山东诚惠快运有限公司-已修复-v2.json`（35 条：网点数组化、四要素反填、tags/toAreas 数组化、补 fromCity/toCity/id）

**验证**：outlets 单测 14/14、adminLine 回归 24/24、searchLine 11/11、node --check 全过。

**数据质量提醒**：17 个可疑位数号码（手机 9~10 位、座机本地号 6 位等），只标记未改写，需与公司核实。

**用户侧待办**：重新上传 adminLine 云函数 + 前端重新编译；v2 文件建议走管理端批量导入（服务端再归一化一遍）并先删旧 35 条避免重复。

## 新增：干净的空白导入模板

`docs/专线导入模板-空白版.json`（新建）—— 首条为 `_说明*` 字段（下划线开头，前端 `isNaN(field)` 过滤，导入时自动忽略），第 2 条起为 19 字段空白记录。
相比旧模板 `专线导入模板-空白填写版.json` 的优势：旧模板第 1 条带 `【填】` 占位符、第 2 条为示例需手工删两条，易被当真实数据导入产生脏数据；新版无占位符、无示例、点开即用。
字段已机器校验与 adminLine `FIELD_WHITELIST` 对齐（不写 fromCityId/toCityId/fromAddress/toAddress/phone/vipExpireAt 这 6 个自动派生字段是正确的）。
`docs/批量导入说明.md` 已补引用说明。

## 详情页网点列表改版（用户截图规范）

按用户截图重排"地址与联系"：📍地址在上（右侧可挂 KM 距离标签）→ 电话号码**横排一行**（flex-wrap 自动换行，点号码直拨）→ **右侧一组一个拨号按钮**（垂直居中）。标题"发站/到站"改"始发地/目的地"，去掉编号徽标。
- `pages/line-detail/index.wxml`：pair 区重构，新增 item.km 显示位（有值才渲染）
- `pages/line-detail/index.js`：新增 `onCallGroup`（组内 1 号直拨 / 2~6 号 ActionSheet / >6 引导点号码）
- `pages/line-detail/index.wxss`：新增 .pair-main/.pair-addr-text/.pair-km/.pair-phones/.phone-chip/.pair-call；删 .pair-no/.pair-row/.pair-body/.contact-value/.phone-text 死样式（顺带消一处硬编码色）
- 颜色沿用品牌蓝令牌（截图为橙色），已向用户说明可再调
- **KM 距离未生效**：数据模型是线路级单坐标（latitude/longitude），无网点级坐标且本批数据全为 0；显示位已预留，将来网点带坐标即可点亮。勿当成 bug。
- 自检脚本 `.workbuddy/scripts/check-detail-layout.js`（事件绑定/类引用/旧类残留/括号配平）
